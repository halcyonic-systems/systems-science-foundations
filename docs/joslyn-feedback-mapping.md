# Joslyn Feedback Mapping: Summer 2025 Paper → Lean Formalization

*Every red-text comment from Cliff Joslyn's review of `Thornton_IS_Summer_2025.pdf`, mapped to what the formalization now provides.*

**Source**: `notes/Thornton_IS_Summer_2025.pdf`
**Date**: Review ~August 2025 | Mapping created 2026-02-17

---

## Comment Mapping

| # | Page | Cliff's Comment | Answer from Formalization | Relevant File |
|---|------|----------------|--------------------------|---------------|
| 1 | 1 | "Fig. 1 should be referenced and described." | Writing/presentation issue. Not addressed by formalization. | — |
| 2 | 3 | "Is BERT an acronym or initialism? If so, spell it out. If not, it shouldn't be upper case." | Yes: **Bounded Entity Reasoning Toolkit**. Already in references as [7]. Spell out at first use. | — |
| 3 | 3–4 | "do you mean the upper case here to be significant?" (on "Ontology") / "I guess so, but here it's in quotation marks?" | Yes — philosophical Ontology (capital O) vs. computational domain ontology (lowercase). Mobus makes this distinction explicitly. The formalization operationalizes it: Bunge is capital-O Ontology (Systems/Core/), the Lean code is the computational ontology. | Systems/Core/*.lean vs. Systems/Mobus/*.lean |
| 4 | 4 | "Thanks, but you should also cite the Wiener original too." | Add: Wiener, N. (1948). *Cybernetics: Or Control and Communication in the Animal and the Machine*. MIT Press. | — (bibliography) |
| 5 | 4 | "Cite something here." (on Ashby's Laws of Requisite Variety) | Ashby, W.R. (1956). *An Introduction to Cybernetics*. John Wiley. Already in references as [12]. Cite explicitly at this location. | — (bibliography) |
| 6 | 4 | "As discussed, all the systems sciences do this, and not just how systems function, but also how they're structured, and the interaction between the two. Think of function as dynamics and structure as statics." | The formalization embodies exactly this distinction. Bunge's `structure' : Set (α × α)` is the static relational structure. Mobus's `FlowNetwork` with capacity labels captures the dynamic/functional aspect. The bridge theorem (`toBunge`) projects dynamics to statics: `totalRelation` discards capacity (the functional content) leaving only structural pairs. | Bridge.lean:130–132, FlowNetwork.lean:toRelation |
| 7 | 4 | "Yes, that's what distinguishes cybernetics, and especially for issues related to circular information flows. One could argue that control is always so circular." | Flow circularity is representable in FlowNetwork (no acyclicity constraint). The `no_self_loops` constraint prevents trivial reflexive edges but allows cycles through multiple nodes. Circular control flows are valid `FlowNetwork` instances. | FlowNetwork.lean:23 (no_self_loops) |
| 8 | 4 | "You may be right, yes." (on cyberneticists emphasizing epistemology over ontology) | Acknowledged. No formalization response needed. | — |
| 9 | 4 | "Not sure why you leave these paragraph-trailing \\\\." | LaTeX formatting issue. Not a formalization question. | — |
| 10 | 5 | "That's strong, cite something?" (on "charlatans" jeopardizing the systems field) | Needs a specific Bunge source. Check Bunge's *Scientific Materialism* (1981) or the preface/introduction to Vol. 4. The formalization doesn't address this directly, but Bunge's combative stance toward pseudoscience is well-documented. | — (needs Zotero/source check) |
| 11 | 5 | "Cite something" (on Klir) + "maybe don't leave out others, e.g. Mesarovic, Takahara." | Klir (2001) [1] already cited. **The formalization now gives precise standing**: Klir's `S = (T, R)` is Eq. 1.1, formalized as `KlirSystem α` in KlirSystem.lean. It is the verified common root of the commuting triangle. Add: Mesarovic, M.D. & Takahara, Y. (1975). *General Systems Theory: Mathematical Foundations*. Academic Press. | KlirSystem.lean (entire file) |
| 12 | 5 | "Like which ones?" (mathematical frameworks) | **Now precisely enumerable from the codebase**: Set theory (Bunge's CES triple — System.lean), graph theory (Mobus's flow networks — FlowNetwork.lean), relation theory (ActsOn, bondage — Bond.lean), order theory (subsystem partial order — System.lean:118–151), inductive types (recursive decomposition — Level.lean). The BRA companion adds automata theory and category theory. | System.lean, FlowNetwork.lean, Bond.lean, Level.lean |
| 13 | 6 | "So BERT is based on Mobus? I don't think you actually say so anywhere. You need a paragraph introducing BERT and Mobus distinctly, then relating them." | Writing/structure issue. The formalization confirms the relationship precisely: BERT implements Mobus's 8-tuple (the same structure formalized in Tuple.lean). The Lean proofs provide machine-checked foundations for BERT's computational ontology. | Tuple.lean (MobusSystem structure) |
| 14 | 6 | "Odd use of the term 'composition', but I can go with it." | Bunge's term, not ours. Formalized as `composition : Set α` in `ConcreteSystem`. The Lean type makes the meaning unambiguous: it's the set of components. | System.lean:36 |
| 15 | 7 | "What do you mean more precisely? 'Set of relations' literally means S is a set of sets of tuples, right? So e.g. S = {S_i}. Tuples implies a Cartesian product, so is it S_i ⊆ C × E, or S_i ⊆ (C ∪ E) × (C ∪ E), or what? For each relation in this set S_i, all such relations?" | **The Lean compiler forced exactly this decision.** `structure' : Set (α × α)` with constraint `structure_on : ∀ p ∈ structure', p.1 ∈ composition ∪ environment ∧ p.2 ∈ composition ∪ environment`. It is a single flat set of pairs on C ∪ E, not a set of relations. This is the precise resolution Cliff was asking for. | System.lean:40, 44–45 |
| 16 | 7 | "I get that i and l are indices into the level (vertical) and then id (horizontal) within a hierarchy, presumably a tree (as distinct from a more general poset, as we discussed today). But somewhere the actual branching structure needs to be indicated, e.g. that S₅,₂ is a child of S₂,₁. How is that done?" | `docs/recursive-component-architecture.md` addresses this directly. The formalization uses Option C: `MobusSystem` is flat (`components : Set α`), and `RecursiveComponent` in Level.lean captures the tree structure separately. The parent-child relationship is Mobus's dotted index notation formalized as the inductive `RecursiveComponent` type (Level.lean:53–58). Option A (mutual induction between MobusSystem and MobusComponent) is the architecturally correct long-term target. | Level.lean:53–58, docs/recursive-component-architecture.md §3 |
| 17 | 7 | "While Mobus never explicitly cites Bunge, his 7-tuple can easily be viewed as a systematic extension of Bunge's formalization" | **This sentence is now formally wrong.** Mobus cites Klir (2001), not Bunge. Bunge cites Klir & Valach (1967). They developed independently from a shared Klir root. The bridge theorem *discovers* compatibility via projection; it does not verify extension. The commuting triangle (KlirSystem.lean) is the precise correction: Mobus → Bunge → Klir = Mobus → Klir by `rfl`. | KlirSystem.lean:triangle_commutes, Bridge.lean header |
| 18 | 8 | Table 1 "Not included" cells — "So you're suggesting that the 'not included' cells could be filled in?" | **Yes.** Bunge *does* formalize flows (Ch. 2–3), transformations (state space framework), history (state trajectories), time intervals, and level structure (Def 1.7–1.8). He doesn't put them in the CES triple. Phase 1 Lean code formalizes several: state functions (State.lean), level structure (Level.lean), assembly/emergence (Assembly.lean). Table should say "formalized elsewhere in Bunge, not in the system definition" rather than "not included." The six information loss categories in Bridge.lean precisely characterize what Mobus adds beyond Bunge's *system definition*, not beyond Bunge's *entire framework*. | State.lean, Level.lean, Assembly.lean, Bridge.lean:134–179 |
| 19 | 8 | "Thanks, this is definitely an important step in the right direction. It would be preferable to first go through each of the 7 components and explain them on their own terms, and illustrate with a very small example like three systems S_{i,l}, like one parent and two children, nothing more." | **The Lean codebase now IS that component-by-component explanation.** Each has its own file with docstrings: FlowNetwork.lean, Environment.lean, Boundary.lean, Interface.lean. For the ISSS presentation, walk through a tiny example (e.g., a biological cell) showing each tuple element, then show the corresponding Lean structure. The Phase 2 modules are literally organized as "first go through each component and explain it on its own terms." | FlowNetwork.lean, Environment.lean, Boundary.lean, Interface.lean, Tuple.lean |
| 20 | 8 | "And all the i,l subscripting gets distracting and tedious, in small example you can just use A, B, C for the three and simplify notation, just for illustrative purposes." | Agreed — presentation issue. The Lean formalization uses type parameters (`α`, `κ`, `μ`, etc.) not subscript indices, which is cleaner. For exposition, use A, B, C as Cliff suggests. | — (presentation) |
| 21 | 8 | "I regret that this is the kind of thing I was hoping to see initially when I tried to read Mobus directly." | The retrospective (`docs/phase1-retrospective.md`) documents exactly where Mobus's prose proved under-specified when formalized. The Lean code is the "precise version" Cliff wanted. | docs/phase1-retrospective.md |
| 22 | 9 | "Cite the page number." (Mobus quote about mathematical origins being inaccessible) | Needs page number verification in Mobus (2022). Not a formalization question. | — (bibliography) |
| 23 | 9 | "As stated above, I think we need a subsection somewhere which says 'BERT is ...', including the relation to Mobus." | Writing/structure issue. The formalization confirms: BERT implements `MobusSystem` (Tuple.lean). The Lean proofs verify the mathematical foundations that BERT's JSON schema operationalizes. | Tuple.lean |
| 24 | 9 | "The proper way to cite here is tricky, it's awkward, but technically this should be a citation like 'here's the original article, but as reprinted in Facets.' We can talk about it." / "Same issue with the citation here." (p. 10) | Bibliography formatting. Not a formalization question. | — |
| 25 | 11 | "The table should be cited explicitly in the text. These two tables are extremely important, much of the meat of the paper. They should be explained in detail, illustrated by the example." | Writing/presentation issue. Table 2 maps Mobus ontological terms to BERT implementation status. The Lean formalization now provides machine-checked definitions for every ontological term in the left column (Environment → Environment.lean, Source/Sink → Interface.lean:EnvSources/EnvSinks, Flows → FlowNetwork.lean, Boundary → Boundary.lean, Interfaces → Interface.lean, etc.). | Environment.lean, Interface.lean, FlowNetwork.lean, Boundary.lean |
| 26 | 12 | "You should provide full details as to how this quantity is defined or derived, or at least more." (on "aggregate economic productivity signal") | Domain/methodology issue for the case study, not formalization. | — |
| 27 | 12 | "Same issue: figure needs to be cited in the text and explained in detail." (Fig. 2) | Writing/presentation. However, Figure 2's structure (system of interest with environment objects as sources/sinks) maps directly to a `MobusSystem` instance: the outer objects are `environment.objects`, classified as `EnvSources`/`EnvSinks` by Interface.lean, connected via `externalFlows` (bipartite). | Tuple.lean, Interface.lean:EnvSources/EnvSinks |
| 28 | 13 | "Same deal on this figure, and we've both noted the font management issues. It's a problem, for sure." (Fig. 3) | BERT UI/presentation issue, not formalization. | — |
| 29 | 13 | "Are there any sources or sinks in this model? I'm struck by how significant they are in terms of the overall methodology, and while you cite them above, you don't explain them or use them?" | **Now formalized precisely.** `EnvSources` and `EnvSinks` in Interface.lean classify environmental objects by flow direction. `EnvDual` captures objects that are both. In the interstate commerce example, Global Supply Networks and International Trade are sources (`EnvSources`), Global Economic Systems is a sink (`EnvSinks`). The `bipartite` constraint on `MobusSystem` guarantees every external flow crosses between environment objects and interface components. | Interface.lean:73–88 (EnvSources, EnvSinks, EnvDual) |
| 30 | 14 | "Same deal on figure." (Fig. 4) | Writing/presentation issue. | — |

---

## Summary Statistics

| Category | Count | Examples |
|----------|-------|---------|
| **Answered by formalization** | 13 | #6, 7, 11, 12, 15, 16, 17, 18, 19, 21, 25, 27, 29 |
| **Writing/presentation** | 10 | #1, 9, 13, 20, 23, 26, 27, 28, 30 (some overlap) |
| **Bibliography/citation** | 7 | #2, 4, 5, 10, 22, 24 |
| **Not addressable by formalization** | 3 | #8, 14, 26 |

## Key Takeaways

1. **Comment #15 is the strongest vindication.** Cliff asked exactly the question the type-checker forces: "is it S_i ⊆ C × E, or S_i ⊆ (C ∪ E) × (C ∪ E)?" The Lean code answers: `Set (α × α)` on `C ∪ E` with an explicit constraint. The compiler demanded the precision Cliff was requesting.

2. **Comment #17 must be corrected in the paper.** "his 7-tuple can easily be viewed as a systematic extension of Bunge's formalization" is formally wrong. The commuting triangle proves they are independent elaborations of Klir's common root.

3. **Comment #19 has been fulfilled.** "go through each of the 7 components and explain them on their own terms" — the Phase 2 Lean modules are literally organized this way (one file per component, with docstrings).

4. **Comment #29 (sources/sinks) was prescient.** Cliff identified their methodological significance. The formalization now provides `EnvSources`, `EnvSinks`, and `EnvDual` with subset theorems, bipartite edge-direction theorems, and the connection to boundary completeness.

5. **13 of 30 comments are now directly answerable from machine-checked Lean code.** For a paper that was written before any formalization existed, this is a strong signal that formalization addresses the questions a careful reader actually asks.

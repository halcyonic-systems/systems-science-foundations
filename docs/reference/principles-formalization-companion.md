# Principles Formalization — Plain English Companion

*What each formalization says, what it means, and what's left*

**Last updated**: 2026-06-09 — **all 12 principles formalized**
**Lean source**: all `Systems/Core/*.lean` (aggregated by `Systems/Core.lean`)
**Technical roadmap**: `principles-formalization-roadmap.md` · **Clean one-line axiom/theorem list**: `../paper/axiom-table.md`

> **Update discipline**: The axiom table below is the PRIMARY output of every formalization session. After `lake build` succeeds on new theorems, update this table FIRST — one plain English sentence per result, no Lean type names. Then update the per-principle sections, tasknote, roadmap, and session file. If the axiom can't be stated in one sentence, the formalization isn't understood yet.

---

## What we're doing

Mobus lists 12 principles of systems science. We're formalizing them in Lean 4 — turning each principle into precise definitions and machine-checked theorems. The goal isn't to verify what systems theorists already know. It's to discover what the principles actually say when you force them to be precise: which are independent axioms, which are theorems derivable from others, and whether the 12 reduce to a smaller set.

---

## Principle 1: Systemness

**Mobus's claim**: "Bounded networks of relations among parts constitute a holistic unit. Systems interact with other systems, forming yet larger systems. The Universe is composed of systems of systems."

### What we formalized

**RecursiveSystem** — A system is a tree where each internal node is a genuine system (with components, environment, bonds, and coherence constraints) and each leaf is a process primitive (an atomic work process that doesn't decompose further). This is Mobus's recursive decomposition equation enriched with Bunge's system semantics at every level.

**WellFormed** — At every level of decomposition, the children correspond exactly to that level's component set. No phantom components (listed in the system but not represented by any child), no orphans (children that don't belong to the composition).

**IsOrganized / IsAggregate** — A set of things is organized if at least two of its members interact (are bonded). An aggregate is the same components with no bonds — a pile, not a system. Every system is organized by definition; assembly (already formalized) is the transition from aggregate to organized.

### What we proved

1. **Decomposition terminates** — you can't decompose forever; every path hits a process primitive.
2. **Closure under decomposition** — if the whole is well-formed, every subsystem you encounter is well-formed too. This is the formal content of "every component is either atomic or itself a system."
3. **Every level is organized** — no mere aggregate hides inside a genuine system.
4. **Closure under composition** — two systems with disjoint compositions and at least one cross-boundary bond compose into a valid supersystem. The composed system's environment is (E₁ ∪ E₂) \ (C₁ ∪ C₂) — the unique minimal environment satisfying `structure_on`.

### The one-line axiom

> *A system is a bounded, organized collection of things, each of which is either a process primitive or itself a system.*

### What we found

The existing Lean code had an inductive tree type (`RecursiveComponent`) that captured "things have parts that have parts" — but said nothing about whether those parts form genuine systems. It was a tree shape without system semantics. The formalization forced us to notice this gap and close it.

**Composition closure is unconditional at the type level.** Neither the disjointness hypothesis nor the interaction hypothesis is needed for the CES coherence proofs. The union-of-compositions, difference-of-environments, union-of-structures construction produces a valid ConcreteSystem for ANY two systems. The hypotheses add physical content — disjointness ensures distinct systems, interaction ensures meaningful composition — but the mathematical construction doesn't require them. This means the space of systems is closed under arbitrary union, not just interaction-mediated composition.

**The environment formula must be union, not intersection.** Intersection breaks `structure_on`: if σ₁ has a structure relation referencing an environmental thing unknown to σ₂, the intersection drops it, but the structure relation still points there. The union formula is the unique minimal environment that keeps all structure relations valid.

### Design notes

- We use Bunge's CES triple (composition, environment, structure) at each level, not Mobus's full 8-tuple. Systemness is about what makes something a system vs. an aggregate — bonds, composition, environment. The 8-tuple adds operational content (flow networks, transforms, time scales) that belongs to later principles.
- The `thing : α` identifier on each node assumes crisp, well-bounded systems. For fuzzy systems (Mobus §2.2), where the boundary is an analytical choice rather than an ontological fact, this assumption strains. A future formalization could weaken it.
- We do NOT assume the universe is a closed system. The top-level node's identifier is structurally orphaned — no parent references it — but we don't assert closure.

---

## Principle 2: Hierarchy

**Mobus's claim**: "Systems are processes organized in structural and functional hierarchies... The next higher layer consists of subsystems composed of components from the lower layer in which component interactions within the subsystem are stronger than interactions between components in other subsystems."

### What we formalized

**TimeScaleSeparation** — Each level in a hierarchy has a characteristic time scale, and lower levels are strictly faster. Formalized as a strictly monotone map from level indices to an ordered time-scale type.

**InteractionStrength** — A quantitative measure of how strongly one thing acts on another. Generalizes the existing binary relation (acts-on-or-doesn't) to a graded notion. Parameterized abstractly — concrete instances might use flow capacity, coupling coefficients, or information transfer rates.

**NearDecomposable** — A system is nearly decomposable (Simon 1962) when its components partition into modules where:
- Every within-module interaction exceeds a threshold
- Every between-module interaction falls below that threshold

This is what distinguishes genuine hierarchy from arbitrary nesting.

### What we proved

1. **Timescale injectivity** — no two levels share a time scale. Each level operates at a distinct tempo.
2. **Within exceeds between** — in a near-decomposable system, pick any two components in the same module and any component in a different module. The within-module pair always interacts more strongly than the cross-module pair. This is what Simon says informally; we proved it.
3. **Conditional time-scale separation** (Simon's implication) — IF interaction strength monotonically determines time scale (`StrictAnti f`), THEN within-module dynamics are strictly faster than between-module dynamics. One-line proof applying `StrictAnti` to `within_exceeds_between`.

### The one-line axioms

> *A hierarchy is a level structure where lower levels are strictly faster.* (TimeScaleSeparation)

> *A system is hierarchical when its components cluster into modules where within-module interaction uniformly exceeds between-module interaction.* (NearDecomposable)

### What we found

These are two separate claims with a conjectured relationship: near-decomposability is the STRUCTURAL CAUSE (strong internal coupling → fast internal dynamics), time-scale separation is the OBSERVABLE CONSEQUENCE (each level runs at a distinct tempo). Simon states this informally. The formal connection requires one additional assumption — that interaction strength determines dynamical speed — which is exactly what Dynamics (Principle 4) should provide.

**Simon's implicit assumption is `StrictAnti f`: stronger interaction → shorter time scale.** The conditional theorem isolates this precisely. The proof is one line — the mathematical content is in the STATEMENT, not the proof. Naming the assumption IS the contribution. The full unconditional version awaits Dynamics (#4), which would provide the concrete `f`. The conditional version identifies EXACTLY what additional assumption converts structural hierarchy into temporal hierarchy.

---

## Principle 3: Networks — complete

Fully formalized in `FlowNetwork.lean`, `Bond.lean`, `Interface.lean`. Directed graphs with capacity labels, source/sink classification, bipartite flow constraints.

**Engineering note — a quantifier direction is half a definition.** `IsBipartiteFlow` quantifies over *edges* (every boundary-crossing flow lands on an interface) and was for a long time read as fully constraining the boundary. It does not: the converse over *interfaces* was absent, so a boundary could declare interfaces that transport nothing, while the docstring cited Mobus's **functional** definition of an interface ("components that transport flows across the boundary"). When a formalization quotes a functional definition but states only a positional one, check both quantifier directions before calling the tier closed. `interfaces_carry_flow` supplies the missing half; `MobusSystem.interfaces_sub_externalNodes` is the containment that could not be stated without it.

**Engineering note — a new constraint owes a separating instance.** Adding a ninth field to `MobusSystem` proves nothing on its own: had the other eight implied it, the build would stay green and the field would be decoration. `interface_converse_independent` (Interface.lean) discharges that debt with concrete data satisfying all five pre-#31 structural constraints and failing `InterfacesCarryFlow` alone — component `2` is declared an interface that no external edge touches. Deliberately *not* the empty system: the witness carries a live external flow `1 → 0` through a genuine interface, so the separation cannot be dismissed as an artifact of a system with nothing flowing. The statement is at the level of the constraints rather than a `MobusSystem` value, because no inhabitant of the structure can exhibit the failure — the field is part of the type — and duplicating the 8-tuple as an eight-field shadow structure would cost a second definition of the ontology's central object to say the same thing.

---

## Principle 4: Dynamics — complete (structural)

Dynamics.lean (482 lines) formalizes everything Mobus means by "systems are dynamic on multiple time scales" at the structural level. The file grew from bridge infrastructure (connecting to Simon's conditional and Simonian complexity) into a full axiomatization across W21.

**What we proved:**

1. **Dynamics compose as products.** `DynamicSystem.compose` gives the composed state space S₁ × S₂ with independent evolution. Both projection theorems proved (`rfl`): the composed dynamics preserves each subsystem's state evolution.

2. **Coupled dynamics generalizes independent.** `CoupledDynamicSystem` captures mutual influence (each law depends on both states). `DynamicSystem.toCoupled` embeds independent dynamics as the special case where each law ignores the other subsystem.

3. **Equilibrium theory is complete.** `IsEquilibrium` (fixed point), `product_equilibrium` (products of equilibria are equilibria), `coupled_equilibrium_iff_fixed` (biconditional — coupled equilibrium ↔ fixed point of combined law), `independent_equilibrium_is_coupled` (independent equilibria are coupled equilibria), `equilibrium_iterate` (equilibria are preserved under iteration — trajectory from equilibrium is constant).

4. **Flows compose.** `Flow` (semigroup action on state space, `AddMonoid T`) with `flow_zero` and `flow_add` axioms. `Flow.compose` gives product flows with both projections proved.

5. **Timescale decomposition is structural.** `TimescaleDecomposition` separates fast (within-module, frozen at equilibrium) from slow (full coupling). Five theorems: fast₁ and fast₂ have the reference equilibrium as a fixed point, slow dynamics is stationary at the reference, and for independent dynamics the fast laws recover the original subsystem laws exactly.

6. **Simon's bridge is complete.** `InteractionDynamicsBridge` names the physical assumption (StrictAnti: stronger interaction → shorter time scale). `NearDecomposable.simon_from_bridge` composes this with the conditional from Level.lean to give full time-scale separation.

**What we found:**

- The dynamics hierarchy is a hierarchy of fixed-point approximations (Finding 9). Independent → coupled → multi-timescale is zeroth-order → exact → approximate. Near-decomposability is the condition that makes zeroth order a good approximation.
- Governance is independent of dynamics (Finding 10). The set point is genuinely new structure — `GovernanceSubsystem.toCoupled` shows exactly what governance adds beyond coupling.

**What's deferred:** Quantitative convergence (metric on state space), stochastic transitions (measure theory), input-dependent evolution (open systems). These are research-level extensions, not gaps in the axiomatization.

**Life-cycle closure** (`Systems/Mobus/Lifecycle.lean`, SSF #30): the above is dynamics on a *state space* with structure held fixed. The life cycle is dynamics whose carrier is the 8-tuple itself — the structure changes. `wellFormed_of_reaches` proves the 8-tuple is closed under lawful change: well-formedness, reified as `WellFormed : PreTuple → Prop` and pinned to `MobusSystem` in both directions, is preserved along any trajectory of admissible transitions. `additive_components_monotone` + `growthStep_additive` + `step_not_additive` locate Mobus's `S_{t+1} = S_t ∪ ⟨ΔS⟩` precisely: it is the Growth regime, and additive regimes cannot generate a trajectory that loses a component — which is why his Decline and Dissolution sections are empty. This is the paper's §4 (Finding 24). Interface removal, Dissolution as a terminal-state theorem, and `F_φ` in Veliov's terms are out of scope and named in the issue.

---

## Principle 8: Governance — Lens Bridge

Governance.lean captures Mobus's HCGS: Homeostat, GovernanceSubsystem, TwoLevelGovernance. The Lens Bridge (Lens.lean, 2026-05-25) adds the categorical cybernetics connection.

**What we proved:**

1. **Lenses form a category.** `Lens.compose_assoc`, `Lens.id_compose`, `Lens.compose_id` — symmetric lenses compose associatively with identity. This gives hierarchical governance composition at arbitrary depth.

2. **A homeostat IS a lens + reference value.** `Homeostat.toLens` maps sensor → get, (error ∘ correct) → put. The set point is baked into the backward channel. `Homeostat.feedbackLaw_eq_lens` proves the feedback law decomposes as get-then-put (observe, then correct) — `rfl`.

3. **Homeostats compose via lenses.** `Homeostat.composeLens` composes two homeostats through their lens structure. This is what `TwoLevelGovernance` does manually; lens composition does it structurally.

4. **TwoLevelGovernance parameterizes the lens.** `TwoLevelGovernance.opsLens_eq` shows the operations homeostat's lens at a fixed coordination state. The coordinator decides *which* lens is active by modifying the set point — this is the additional governance structure that lenses alone don't capture.

**What we found:**

- The Conant-Ashby theorem's structural skeleton is the walking arrow. `ConantAshbySkeleton` defines a regulator with a model homomorphism `model : R → S` such that `observe ∘ model = regView` (the commuting diagram). This is the same structure as K ≅ **2**: a system is a morphism, and a good regulator is a morphism. The irreducible categorical content of "being a system" and "governing a system" is **2**.

- Three modern frameworks (Capucci et al. categorical cybernetics, Spivak-Niu polynomial functors, Myers CST) all identify the lens as the primitive. Our formalization appears to be the first to connect Mobus's HCGS to this literature in a proof assistant.

- Asymmetric lens composition (where forward and backward types differ) requires profunctor optics — a future extension if needed. For governance, symmetric lenses suffice because the observation and correction share a type.

---

## What the axioms look like (complete — all 12)

Each principle as a one-line axiom or theorem (the full machine-checked set; a clean grouped version for figures lives in `../paper/axiom-table.md`):

| # | Principle | Axiom (candidate) |
|---|-----------|-------------------|
| 1 | Systemness (identity) | A system is a bounded, organized collection, each component of which is primitive or itself a system |
| 1 | Systemness (closure) | Any two systems compose into a supersystem — unconditional at both CES and 8-tuple levels |
| 2 | Hierarchy (structure) | A system is hierarchical when within-module interaction uniformly exceeds between-module interaction |
| 2 | Hierarchy (dynamics) | In a hierarchy, lower levels are strictly faster |
| 3 | Networks | A system's internal structure is a directed flow network with capacity constraints |
| 3 | Networks (boundary, both directions) | Every flow crossing the boundary passes through an interface, **and** every interface carries such a flow — so the interfaces are exactly the component-side endpoints of the external flows, and a system with no external flows declares no interfaces |
| 3 | Networks (boundary converse is independent) | The interface-side requirement is not implied by the flow-side one: there is data meeting every other 8-tuple constraint in which a declared interface transports nothing, so the constraint does real work rather than restating what was already true |
| 4 | Dynamics (state) | A system has a state space and a law governing how state evolves |
| 4 | Dynamics (composition) | When systems compose, their state spaces multiply and their dynamics combine independently |
| 4 | Dynamics (coupling) | Coupled dynamics generalizes independent: each subsystem's evolution depends on the other's state |
| 4 | Dynamics (equilibrium) | Equilibria compose as products; coupled equilibria are fixed points of the combined law; independent equilibria are coupled equilibria |
| 4 | Dynamics (flow) | A flow is a semigroup action on state space — deterministic evolution parameterized by time, composable as products |
| 4 | Dynamics (timescale) | Coupled dynamics decomposes into fast (freeze other subsystem, evolve independently) and slow (full coupling) around any equilibrium |
| 4 | Dynamics (bridge) | Stronger interaction produces faster dynamics — this is what converts structural hierarchy into temporal hierarchy |
| 4 | Dynamics (life cycle) | The 8-tuple is closed under lawful change: well-formedness is preserved by every admissible transition, hence along trajectories of any length. Mobus's `S_{t+1} = S_t ∪ ⟨ΔS⟩` is the Growth regime of that transition relation — additive, so no trajectory it generates ever loses a component |
| 5 | Complexity | **Not an axiom.** Structural measures derive from #1+#2+#3. Simonian state-space reduction bridges #1+#2 to #4. Behavioral complexity is Mobus's own open problem. |
| 6 | Evolution | **Axiom (independent), blind.** A system evolves under an *environmental* fitness order (a preorder — fitness is relational, "not resident in some mind", per M&K §10.2.1.4): a generational step that is fitness-non-decreasing, so fitness climbs monotonically across all generations. Requires no model, goal, or understanding — that blindness lets it run on *any* system. The blind counterpart of directed improvement (#12): they share the ontogenic variation→selection skeleton but #6's criterion is environmental, #12's goal is mental. The prime-cyclic system is evolvable yet not improvable (blind selection succeeds where directed engineering can't). Mobus's "#12 corollary of #6" is reversed, like "#11 corollary of #9". |
| 7 | Information (genus) | Information is a difference that makes a difference (Bateson): a message is informative iff it changes the receiver's state. Knowledge is the state it updates — the internal model of #9. Probability-free; a non-informative message is exactly an equilibrium of that message's action. |
| 7 | Information (Shannon is a special case) | Shannon entropy is not the genus of information but a bounded special case: equip a finite outcome set with a distribution and entropy is at most the (probability-free) Hartley nonspecificity `log(card)`, with equality only at the uniform distribution. Adding probability to the Hartley ceiling can only lower the measure — Shannon sits strictly below Hartley, which sits below the relational genus. |
| 8 | Governance (homeostat) | A governed process has a reference state (set point), a sensor, an error function, and a corrective action — the feedback loop |
| 8 | Governance (asymmetry) | Governance is coupled dynamics plus a reference state — the set point is genuinely new structure not in Dynamics #4 |
| 8 | Governance (hierarchy) | Higher governance levels modify lower levels' set points — feed-downward maps to TimescaleDecomposition with goals at each level |
| 8 | Governance (lens) | A homeostat IS a bidirectional lens (observe/correct) equipped with a reference value — the set point parameterizes the backward channel |
| 8 | Governance (Conant-Ashby) | A good regulator contains a homomorphic model of the system it regulates — the walking arrow connects governance to K ≅ **2** |
| 9 | Internal Models | **Theorem, not an axiom.** A subsystem that holds a model of another system and simulates it one step ahead necessarily predicts it at every future step — one-step correctness lifts to all horizons. Anticipation is structural, not an extra feature. The model map is exactly the homomorphism a good regulator needs (#9 supplies #8). The Rosen fast-model version is also a theorem: a model that advances `lead` system-steps per internal tick has raced `n·lead` steps ahead after n ticks (`AnticipatoryModel.tracks`), and the lockstep internal model is exactly the `lead = 1` case (`InternalModel.toAnticipatory`). |
| 10 | Self-Models | **Theorem (tractable core), not an axiom.** A self-model is the diagonal case of #9 — a system modelling its own dynamics — so a correct one-step self-model predicts the system's own future at every horizon (self-anticipation, inherited from #9). Existence is trivial (the identity self-models any system), so the content is faithfulness, not existence; and a self-image the model gets right stays right as the system runs (the accurate set is dynamics-invariant). The existence/obstruction question for a *faithful* self-model (Lawvere/diagonal) is deferred. The fast (Rosen) diagonal adds a tension theorem lockstep lacks: accuracy and speed fight. A fast self-model accurate along an orbit segment collapses the fast clock onto the slow one (`accurate_collapse`) and forces the reached state into periodicity (`accurate_forces_periodic`) — perfect self-anticipation is only consistent with a nearly-static future. Accuracy invariance is a lockstep privilege: the doubling witness (`ℕ`, successor, `lead = 2`) is accurate at 0 but not at 1. |
| 11 | Understandability | **Not a corollary of #9 — it carries its own axiom: models must be simpler than the modelled.** To understand a system is to have a model strictly *simpler* than it: an onto, **lossy**, **non-degenerate** coarse-graining (distinct system states collapse, but the model keeps ≥2 distinguishable states — the total point-collapse is excluded) whose dynamics commute. From that, a correct one-step abstraction predicts the system at every horizon, and on finite carriers the model is strictly smaller (`card M < card S`, strictly lower Hartley than #7). Mobus calls #11 a corollary of #9, but #9 is satisfied for free by the identity model, which simplifies nothing. Two witnesses show #9 ⇏ #11: a one-state system (nothing to compress) and the **3-cycle on three states** (compression forbidden — 3 is prime, so the only dynamics-respecting quotient is the excluded point-collapse). The compression requirement is independent content; if anything the arrow reverses (to understand is, a fortiori, to model). #9 *decodes* (representation → reality); #11 *encodes* (reality → representation) — dual. |
| 12 | Improvability | **Not a corollary of #6 — it is directed (agential), where Evolution is blind.** To improve a system is for an external agent to *understand* it (#11) and *intervene from outside its boundary* — rewrite the dynamics so a chosen **goal** (a set-point not supplied by the system) becomes a rest state it was not before. The directed outcome is stable at every horizon. The goal is genuinely external: the same dynamics admit improvements toward *different* goals (`goal_is_external`), so improvement is not a function of the system. **#12 ⟹ #11** by construction (the agent carries an understanding); governance (#8) is the realized engine (`Homeostat.toImprovement` — set-point = goal, feedback = intervention). And **#12 ⇏ #6**: the prime-cyclic 3-cycle has a model but no understanding, hence no directed agent — it can be blindly evolved but not deliberately improved (`cyclic3_no_directed_improvement`). #11 = observe/GET, #12 = intervene/PUT: together the agent's bidirectional lens. Mobus's "corollary of #6" is misplaced as #11's "corollary of #9" was. |

The axiomatization question: how many of the 12 are independent? If Complexity derives from the structural principles, we're already down to fewer than 12. The ultimate deliverable is a dependency DAG showing which principles are axioms and which are theorems.

---

## Findings the prose tradition doesn't contain

The formalization is systematically producing results that are stronger than, differently structured from, or absent in the existing literature — including Bunge's set-theoretic definitions, Mobus's informal principles, and Simon's verbal arguments. These are not corrections — they are discoveries that only become visible under machine-checked proof.

1. **Composition closure is unconditional.** Mobus says "systems interact with other systems, forming yet larger systems." The formalization says the interaction isn't even required — the CES construction produces a valid system from ANY two systems. The disjointness and interaction hypotheses add physical meaning but are mathematically unnecessary. The space of systems is closed under arbitrary union, not just interaction-mediated composition. This is stronger than the principle claims.

    *What this means practically:* At Bunge's CES level, the boundary of what counts as "a system" is the modeler's choice — composition can't fail. But this permissiveness has limits: at Mobus's 8-tuple level, composition must respect flow constraints, boundary completeness (B = ⟨P, I⟩), and interface bipartiteness. The constraints that make composition non-trivial come from Mobus's extensions (flows, boundaries), not from Bunge's foundation (CES). Composition is free at the foundation; the 8-tuple is where the boundary constrains you. A composed system of non-interacting parts is valid at the CES level but boring — an aggregate of systems. The cross-boundary bonds are what create emergent structure. For BERT: composition at the structural level should be unconstrained; flow and boundary validation is a separate, later check.

2. **Simon's bridge has a named gap.** Simon's 1962 argument jumps from *how tightly coupled* components are (structural) to *how fast* things change (temporal). That jump requires an unstated assumption: coupling strength determines dynamical speed. This is usually true — tightly coupled springs oscillate faster, strongly connected neurons synchronize quicker, closely linked markets equilibrate sooner. But it is not a logical necessity. Tectonic plates are tightly coupled and move slowly. The formalization isolates the assumption precisely: a strictly anti-monotone map from interaction strength to time scale. It proves: IF this map exists, Simon's conclusion follows. If it doesn't, the argument breaks. Hierarchy is really two independent claims glued by this assumption, and Dynamics (#4) is what provides it. For 60 years, systems theorists have used Simon's argument as if the structural and temporal claims are the same thing. They are not.

    *What this means practically:* It tells you *when* Simon's argument applies (domains where coupling → speed, like springs, circuits, markets) and *when* it doesn't (domains where coupling and speed are independent, like geology, certain institutional systems). When building hierarchical models, the question is not just "are these modules tightly coupled?" but "does tighter coupling actually produce faster dynamics in this domain?"

3. **The environment formula is forced.** When composing two systems, the supersystem's environment must be (E₁ ∪ E₂) \ (C₁ ∪ C₂). This isn't a design choice — intersection provably breaks the `structure_on` coherence constraint. The union formula is the unique minimal environment that keeps all structure relations valid. This kind of uniqueness result doesn't exist in the prose tradition, where environment is defined informally.

4. **Complexity decomposes into other principles — under Mobus's own scoping.** Mobus (Ch. 4) distinguishes structural complexity from Simonian complexity (state-space explosion tamed by near-decomposability) and explicitly defers behavioral complexity as future research. The formalization shows each piece is accounted for: structural complexity derives from #1+#2+#3 (proved in Complexity.lean with only Core imports). Simonian complexity is a bridge theorem connecting Hierarchy (#2) to Dynamics (#4) — structurally parallel to Simon's conditional, it names what Dynamics provides without requiring a new axiom. Behavioral complexity is Mobus's own open problem, not part of the stated principle. No part of Complexity as Mobus scopes it requires new axioms. The 12 reduce by at least 1.

5. **Diversity derives from ActsOn, not from a new typing system.** The session plan flagged component-kind diversity as the potential independence locus. The formalization resolves this: two components are "of the same kind" iff they have identical interaction profiles (SameKind equivalence). This is definable from ActsOn alone — the equivalence relation, its reflexivity/symmetry/transitivity, and the well-definedness of equivalence classes all derive from Systemness. No new type parameter or classification system needed.

    *What this means practically:* BERT and GSR could infer component types from interaction patterns rather than requiring manual labels. Two components that interact with the same things in the same ways ARE the same kind — no taxonomy needed.

6. **Complexity lives in Core, not Mobus.** The session plan pre-resolved file placement as `Systems/Mobus/Complexity.lean` (complexity uses 8-tuple data). The formalization proves this wrong: the CES triple suffices. The file is `Systems/Core/Complexity.lean`. The placement IS the derivability argument — if it compiles with only Core imports, complexity doesn't need the 8-tuple.

7. **8-tuple composition is also unconditional.** We predicted that Mobus's boundary constraints (bipartite external flows, boundary completeness, interface containment) might make composition conditional — that the boundary would be the point where "modeler's choice" ends. The formalization proves otherwise: MobusSystem.compose compiles with the same hypotheses as CES-level compose (disjoint components only). The bipartite property transfers because composition only REMOVES external edges (reclassifying them as internal when both endpoints become components), never adds them. The remaining edges still cross between environment and interfaces. Mobus's boundary is an organizational tool, not a composition gate.

    *What this means practically:* The entire Mobus 8-tuple — flow networks, boundaries, interfaces, capacity labels — imposes no composition constraint beyond what the minimal CES triple requires. BERT model composition at the full 8-tuple level is as free as at the structural level. Validate flow structure AFTER composing, not as a precondition.

8. **Unconditional composition is structurally inevitable, not accidental.** The reason composition works at both CES and 8-tuple levels goes deeper than "the proofs happen to go through." All constraints in both formalisms are either UNIVERSAL (for all edges/components, P) or EXISTENTIAL-ABOUT-INTERNALS (there exist bonded pairs in the composition). Universal constraints are preserved when edges are removed. Internal existential constraints are preserved because composition doesn't modify internal structure. Composition only removes external edges (reclassifies as internal) and never adds edges. So ANY constraint of these two forms is composition-safe. This is a design property of Bunge's and Mobus's formalisms — and neither of them knew it.

    *What this means practically:* Any future extension to the 8-tuple that uses universal or internal-existential constraints will also compose unconditionally. Composition safety is a structural invariant of the formalism, not something that needs to be re-verified for each new constraint.

9. **The dynamics hierarchy is a hierarchy of fixed-point approximations.** The coupled equilibrium theorem (`coupled_equilibrium_iff_fixed`) reveals that coupled equilibrium is not a new concept — it's just equilibrium of the combined law on the product state space. The coupling doesn't create new kinds of stability; it changes which function you're finding fixed points of. This means the entire dynamics hierarchy — independent → coupled → multi-timescale — is a hierarchy of fixed-point approximations: product equilibria are zeroth order, coupled equilibria are exact, and near-decomposability is the condition that makes zeroth order a good approximation. Multi-timescale decomposition, when formalized, will be: coupled fixed points are "close to" product fixed points when coupling is weak.

    *What this means practically:* This gives multi-timescale decomposition a clean formal target: a metric on equilibria parameterized by coupling strength, with near-decomposability as the bound. For BERT, it means model quality can be measured by how close the coupled equilibrium is to the product equilibrium — a computable proxy for "how well-decomposed is this model?"

10. **Governance is independent — the set point is the new primitive.** CoupledDynamicSystem captures mutual influence between subsystems. GovernanceSubsystem extends it with a reference state (set point), error function, and asymmetric feed-downward. The `toCoupled` forgetful map shows the exact structure governance adds: when you forget the set point and error function, you recover plain coupled dynamics. The forgetting IS the proof that governance adds something dynamics doesn't have. Goal-directed regulation — the feedback loop with a reference state — is genuinely new structure.

    *What this means practically:* When modeling in BERT, a governance subsystem is not just "two things that influence each other." It's a regulator with an intended state. BERT should distinguish governed processes (with set points) from merely coupled ones. The set point is user-specifiable — it's what the modeler says the system is TRYING to maintain.

11. **Governance and systems share a categorical root — the walking arrow.** The Conant-Ashby theorem (1970) says a good regulator must contain a homomorphic model of the system it regulates. The formalization reveals this is the walking arrow: a morphism R → S making an observation diagram commute. K ≅ **2** says a system IS a morphism (relations depend on things). Conant-Ashby says governance IS a morphism (the regulator models the system). The irreducible categorical content of "being a system" and "governing a system" is the same structure: **2**. The `Homeostat.toLens` decomposition makes this concrete — the feedback law is get-then-put, the bidirectional channel of a lens. Three independent modern frameworks (Capucci et al. categorical cybernetics, Spivak-Niu polynomial functors, Myers CST) all converge on this same primitive. Our formalization appears to be the first to connect Mobus's HCGS to categorical cybernetics in a proof assistant.

    *What this means practically:* The lens decomposition tells you that every governance structure has exactly two channels: a forward observation channel and a backward correction channel, parameterized by a reference value. For BERT, this means governance subsystems should be modeled as bidirectional connections with an explicit set point, not as generic coupled processes. The Conant-Ashby connection means the regulator's "model" of the system is formally the same structure as the system itself — the walking arrow at every level.

12. **Anticipation is structural, not an advanced feature — and Internal Models needs no new axiom.** Mobus lists internal models as a principle and notes they range "up to complex anticipatory models," which reads as: anticipation is a sophisticated capability some models have. The formalization shows anticipation is *automatic*. Any internal model whose dynamics simulate the system for ONE step (the commuting square `model ∘ internalDyn = systemDyn ∘ model`) necessarily predicts it for ALL steps (`InternalModel.tracks`, proved by induction on the horizon). There is no separate "anticipatory" axiom — a correct one-step model is already a correct n-step predictor. Moreover, Internal Models is a **theorem-tier** principle: the model map is just a dynamics homomorphism (`Function.Semiconj`), built entirely from Dynamics (#4) with no new primitive — so realizing #9 adds no axiom (a second reduction after Complexity). And the model map is *exactly* the homomorphism a good regulator must contain: `InternalModel.toConantAshby` shows every internal model induces the Conant-Ashby skeleton, so #9 supplies precisely the structure #8 requires. Modelling a system and governing a system are the same walking arrow. *Addendum (2026-07-10):* the genuinely Rosen version — a model running FASTER than its system — is also theorem-tier. `AnticipatoryModel` replaces the one-step square with `model ∘ internalDyn = systemDyn^[lead] ∘ model` (one internal tick = `lead` system steps), and `AnticipatoryModel.tracks` shows the lead compounds: after n ticks the model has raced `n·lead` steps ahead — same induction skeleton as lockstep, with `iterate_add_apply` collapsing the composed iterates. `InternalModel.toAnticipatory` closes the round-trip: the lockstep model is exactly `lead = 1`, so "anticipatory" is a strict generalization, not a sibling. Still no new axiom, still choice-free.

    *What this means practically:* For BERT/GSR, model fidelity is a **one-step property to check** — if a subsystem correctly tracks another for a single step, it is automatically a valid predictor at any horizon; you don't separately validate "anticipation." And a governed process's internal model and the regulator's required model are the same object, so governance and prediction reuse one structure.

13. **A self-model is the diagonal of an internal model — and existence is the wrong question.** Self-Models (#10) is what you get from #9 by setting the modelled system equal to the container: one state space `S`, with `selfModel : S → S` simulating S's *own* dynamics (`InternalModel S S` with `internalDyn = systemDyn = dyn`). Because it is literally a special case, every #9 result transfers for free through `SelfModel.toInternalModel`: a correct one-step self-model predicts the system's own future at every horizon (`SelfModel.tracks` — self-anticipation), and self-modelled fixed points stay fixed (`equilibrium_image`). The formalization also reframes the principle: **existence of a self-model is trivial** — the identity commutes with any dynamics (`SelfModel.trivial`), so *every* system self-models itself degenerately. The scientific weight is therefore on faithfulness/non-triviality, not existence. The one genuinely self-referential result the set-theoretic core supports is that **self-consistency is dynamics-invariant**: a state the model represents as itself (`selfModel s = s`) is still represented as itself after a step (`accurate_invariant`, since `selfModel (dyn s) = dyn (selfModel s) = dyn s`) — the self-image, once correct, stays correct as the system runs. **Theorem-tier, no new axiom** (it reuses #9's machinery). *Deferred (research-level):* the existence/obstruction theorem for a *faithful* self-model — whether a proper part can perfectly model the whole — needs Lawvere's fixed-point theorem; Mathlib carries only the powerset diagonal (`Function.cantor_surjective`), so the categorical limitation is a separate build. *Addendum (2026-07-10):* the fast (Rosen) diagonal — `FastSelfModel`, a system whose self-model gains `lead` steps of its OWN dynamics per tick — exposes a tension invisible at lockstep: **accuracy fights speed**. `accurate_invariant` (a correct self-image stays correct) is a `lead = 1` privilege, refuted at `lead = 2` by the doubling witness (`ℕ` under successor, self-modelled by doubling: accurate at 0, wrong at 1). And what survives of accuracy is rigid: a fast self-model accurate at both ends of an orbit segment collapses the fast clock onto the slow one (`accurate_collapse`: `dyn^[n·lead] s = dyn^[n] s`), forcing the reached state into periodicity with period dividing `n·(lead−1)` (`accurate_forces_periodic`). **Perfect self-anticipation collapses time**: a genuinely fast, faithful self-oracle can only exist over a nearly-static (eventually periodic) future — the poor man's Lawvere obstruction, obtained with iterate arithmetic instead of fixed-point machinery. Theorem-tier, choice-free, no new axiom. *Engineering note (`accurate_forces_periodic`):* **`Nat.pred` and `- 1` are different syntax to `rw`** — `Nat.succ_pred_eq_of_pos` failed to match a goal stated with `- 1`; rather than hunting the dialect-bridging lemma, reduce iterate-index equations to a bare linear goal (`congr 1`) and let `omega` close it.

    *What this means practically:* For BERT/GSR, a system's self-model is not a new kind of object — it is its internal-model machinery pointed at itself, so reflective/self-monitoring subsystems reuse the same one-step-fidelity check and the same equilibrium guarantees. And "does the system have a self-model" is never the interesting flag (it always does, trivially); the flags worth surfacing are how faithful the self-model is and which states it represents accurately (those states being closed under the dynamics).

14. **The Conant-Ashby Good Regulator theorem's engine is the strict concavity of −x·log x — now machine-checked.** Conant & Ashby (1970) prove the *information-theoretic* result that the simplest *optimal* (entropy-minimizing) regulator is a deterministic mapping `h: S→R` — a model of the system. The entire force of their proof is one entropy fact: shifting outcome probability mass to make `p(Z)` more unequal strictly lowers `H(Z)`. Formalized (`GoodRegulator.lean`, `negMulLog_transfer`), that fact is exactly the strict (Schur-)concavity of `negMulLog`: a fixed-sum transfer toward greater imbalance strictly decreases `negMulLog a + negMulLog b`. This is the genuine theorem's core, and reading the 1970 paper directly was decisive — it showed the earlier `ConantAshbySkeleton` (and the `InternalModel` simulation result) are the *structural/representational* direction (R→S), whereas the theorem's actual mapping is the dual *determination* direction `h: S→R`, forced by entropy. The determinism wrapper (an optimal regulator can't spread one state's mass over two outcomes without lowering entropy → it must be a mapping) is scoped with a full proof strategy in the file; the hard, novel part — the entropy engine — is verified.

    *What this means practically:* "A good regulator is a model of the system" is not a metaphor or a structural convenience — it is *forced by entropy minimization*, and the forcing mechanism is a one-line concavity fact now in the library. This grounds the K ≅ **2** "governing = modelling" claim in the original information-theoretic theorem, not only the walking-arrow skeleton.

15. **Shannon is a special engineering case of information, and the type hierarchy proves it.** Principle 7 is formalized in three layers, each requiring strictly more structure than the last (`Information.lean`). (i) *Genus, probability-free*: information is a difference that makes a difference (Bateson) — a message is informative for a receiver iff it changes the receiver's state (`Channel.Informative`), and knowledge is the state it updates, i.e. the internal model of #9 (`InternalModel.toChannel` exhibits the autonomous model as the input-free channel; a non-informative message is exactly an equilibrium of that message's action). (ii) *Hartley, set-based, still probability-free*: nonspecificity `log(card)` quantifies how many outcomes could be resolved, with no distribution. (iii) *Shannon, probability-based*: expected information `entropy p`, and the headline refinement `entropy_le_hartley_univ` — entropy is bounded **above** by Hartley, with equality only at the uniform distribution (`entropy_uniform_eq_log_card`, proved via Jensen on the concave `negMulLog`). So adding a probability distribution to the prob-free Hartley ceiling can only *lower* the information measure, and recovers Hartley only under maximal ignorance. This is the precise, machine-checked sense in which Shannon's communication-statistics is a bounded special case sitting *below* a more general, structure-light notion — exactly Klir's Generalized-Information-Theory stance, not Shannon-as-definition. The top of the stack (semantic / viability information — meaning and goal-relevance, refining the genus by a set-point à la #8) is left deliberately open. **Theorem-tier, no new axiom.**

    *What this means practically:* For Halcyonic/BERT, "information" should never be hardcoded as Shannon bits. The genus to check first is qualitative (did the message change the receiver's knowledge-state?); entropy/capacity are an optional quantitative layer that applies only once a distribution is supplied, and they are provably bounded by the prob-free nonspecificity. This keeps the door open to the eventual semantic/viability measure without having to tear out a Shannon-as-definition commitment.

16. **Understandability is not a corollary of Internal Models — it carries a hidden compression axiom, and the arrow Mobus drew points the wrong way.** Mobus lists #11 as "a corollary of #9." The formalization (`Understanding.lean`) shows it is independent. The move is to take seriously what "understand" adds over "model": a model that is no simpler than the thing modelled has *duplicated*, not *understood*, it. So an `Understanding` of `S` is an abstraction `abstract : S → M` whose model dynamics commute one step (`abstracts`), and which is **onto** (`surjective` — no junk model states), **lossy** (`compresses` — `¬ Injective`, distinct system states share a model state), and **non-degenerate** (`nontrivial : Nontrivial M`). From the one-step commuting square, a correct abstraction predicts the system at every horizon (`Understanding.tracks` / `predict_correct` — running the *simple* model forward equals the true future, coarse-grained), preserves rest states (`equilibrium_image`), and on finite carriers is **strictly smaller**: `card M < card S` (`card_lt`) and hence strictly lower Hartley nonspecificity `hartley univ_M < hartley univ_S` (`hartley_lt`) — the compression of #11 *is* the Hartley drop of #7, made quantitative.

    *The `nontrivial` field is itself a finding.* Without it the definition is degenerate: the **total collapse** `abstract : S → Unit` is onto, lossy, and its square commutes vacuously, so *every* ≥2-state system would be "understood" by discarding all its structure. A model that distinguishes nothing predicts nothing. Requiring `Nontrivial M` excludes the point-collapse and forces a *proper* coarse-graining — strictly between the identity (no compression) and the point (no information).

    *Independence (#9 ⇏ #11), two witnesses.* **Every system models itself for free** — `InternalModel.refl` builds the identity InternalModel for any dynamics — but that same identity map can never be an Understanding (injective, fails `compresses`: `no_trivial_understanding`). (i) *Minimal:* a one-state system has a model but no understanding — nothing to compress (`modeling_does_not_imply_understanding`, via `no_understanding_of_subsingleton`). (ii) *Strong:* the **3-cycle on three states** (`x ↦ x+1` on `Fin 3`) has a model yet cannot be understood (`cyclic3_no_understanding`) — not for lack of room (it has three states) but because **3 is prime**: any dynamics-respecting identification of two states is forced by the cycle to identify all three, collapsing the model to a point, which `nontrivial` forbids. Understanding can be impossible even for richly dynamic systems; the obstruction is the *dynamics*, not the cardinality. So #9 is trivially satisfiable while #11 is a real constraint; if anything the dependency reverses (to understand is, a fortiori, to model). The two are also *dual*: #9 *decodes* (`R → S`, representation → reality), #11 *encodes* (`S → M`, reality → representation, à la Rosen). Non-vacuous via `noisyPairUnderstanding` (conserved coordinate kept, noisy one discarded). **Axiom-tier** — the compression requirement is genuine new content, the "hidden epistemological axiom" the roadmap predicted: *models must be simpler than the modelled.*

    *What this means practically:* For BERT/GSR, "the system has a model of X" and "the system understands X" are different flags and must be checked differently — the first is nearly free, the second demands a strict, *non-degenerate* coarse-graining (a quotient with fewer-but-≥2 states whose dynamics still close). A reflective subsystem that mirrors another at full resolution is modelling, not understanding; a model that lumps everything into one bucket is not understanding either. And some systems are *unintelligible by construction* — prime-cyclic dynamics admit no informative quotient at all, a formal caution against assuming every system has a simplifying model. This also gives the compression bridge to Complexity (#5): the fibres of `abstract` are exactly the lumping `SameKind` performs on components — understanding is structural complexity, reduced.

17. **Improvability is not a corollary of Evolution — it is the agential dual of Understanding, and the 12 principles split 10 ontological + 2 agential.** Mobus lists #12 as "a corollary of #6 (Evolution) — Engineering." The formalization (`Improvability.lean`) shows it is independent and *agential*. Evolution is blind (random variation, environmental selection); improvement is *directed* — an external agent that *understands* the system (#11) and *intervenes from outside its boundary* to rewrite the dynamics so a chosen **goal** becomes a rest state. An `Improvement S` is `dyn`, a `goal`, and an `intervene : (S→S)→(S→S)` (a map ON the dynamics — the externality #4's fixed law and #6's internal change both lack) with `improves` (goal is now an equilibrium) and `genuine` (it was not before); the directed outcome is stable at every horizon (`persists`, via `equilibrium_iterate`). Three results carry the finding. (i) **The goal is external structure:** the same dynamics admit improvements toward *different* goals (`goal_is_external`, witnessed on `Bool` under `not`), so improvement is not a function of the system — it adds the goal + intervention no ontological principle supplies. (ii) **#12 ⟹ #11:** a `DirectedAgent` *carries* an `Understanding` of the system it acts on (`toUnderstanding`) — you cannot deliberately improve what you do not understand; and governance (#8) is the realized engine — `Homeostat.toImprovement` makes the set-point the goal and the feedback law the intervention, reusing `target_is_equilibrium`. (iii) **#12 ⇏ #6:** the prime-cyclic 3-cycle on `Fin 3` has a model but no understanding (finding #16), so it admits **no directed agent** (`cyclic3_no_directed_improvement`, one line off today's `cyclic3_no_understanding`) — it can be *blindly evolved* but not *deliberately improved*. The structural payoff — the roadmap's "most interesting" question, answered: **#11 = GET/observe (`abstract : S → M`, read state out) and #12 = PUT/intervene (rewrite the dynamics, write structure in) are the two channels of the designer's lens onto a system** (the homeostat already carries both — `toLens` get/put, `toImprovement` the directed put). The 12 principles are 10 about what systems ARE/DO + 2 about what an agent does WITH a system, and the two agential principles interact (improvement needs understanding). **Axiom-tier, independent.**

    *What this means practically:* For BERT/GSR this licenses a clean separation of *descriptive* from *agential* tooling. Modelling (#9), understanding (#11), and the ontological core describe a system; improvement (#12) is a distinct capability that needs an explicit, external goal/set-point plus an intervention operator — it should never be inferred from the dynamics alone (the same system supports contradictory "improvements" depending on the goal). The observe/intervene (get/put) pairing says an agent UI over a model wants both a read channel (the understood coarse-graining) and a write channel (goal-directed intervention), and the homeostat is the ready-made both-at-once primitive. And the hard limit is worth surfacing: a system you cannot compress to a simpler model is one you can only let evolve, not engineer.

18. **Evolution is the blind axiom — the third pillar, and it completes the 12.** Mobus calls #12 a corollary of Evolution (#6); having shown #12 is an independent *agential* axiom, we formalize #6 (`Evolution.lean`) as the independent *blind* one. Grounded in Mobus & Kalton 2015 §10.2.2 ("Evolution as a Kind of Algorithm") and §10.2.1.4 ("Fit and Fitness" — fitness is "inherently relational, no meaning without the environment"), an `Evolution S` over an environmental fitness order `[Preorder S]` is a generational `step` with the single law `selects : ∀ s, s ≤ step s` (each generation at least as fit). From it, `adapts`: fitness is non-decreasing across *all* generations (one-step lifts to all-horizon, the #6 analogue of #11's `tracks` and #12's `persists`). `Evolvable` captures the CAES *capacity* (a state that strictly improves), distinct from the act; `IsAdapted` is a fitness peak (the fixed point the climb approaches). The structure deliberately has **no model, goal, or understanding** — the decisive M&K line is that evolution's criterion is "**not resident in some mind** but in the conditions imposed by the surrounding system." That blindness is the whole point: it lets #6 run on *any* system, and it is exactly what separates it from #12 (whose goal lives in an agent's mind). The seam is made concrete by **`evolvable_but_not_improvable`**: the prime-cyclic 3-cycle — which has a model but no understanding (#16) and so no directed agent (#17) — is nonetheless **evolvable**. A prime-cyclic system can be blindly evolved but not deliberately improved. Mobus's three ontogenic loops (auto-organization / biological evolution / intentional-organization) are one variation→selection skeleton instantiated blind (#6) or directed (#12); neither reduces to the other, so "#12 corollary of #6" is misplaced just as "#11 corollary of #9" was. **Axiom-tier, independent — and with it all 12 principles are formalized.**

    *What this means practically:* This closes the program and fixes the top-level taxonomy for BERT/GSR: **10 ontological principles** (what systems ARE and DO) + **2 agential** (#11 observe, #12 intervene) + the **blind/directed axis** on structural change (#6 evolution vs #12 engineering). "Can this system be improved?" and "can it evolve?" are different queries with different preconditions — improvement needs an understanding and a goal; evolution needs only an environmental fitness order. A system can be evolvable without being understandable, which is the formal content of "let it evolve" vs "engineer it." The 12 are now ready to be drawn as a dependency DAG (which principles are independent axioms, which are derived theorems).

19. **Principles 6–12 are not "the complex-adaptive-systems principles" — the real boundary is the model line (#9), and #11/#12 float free of complexity entirely.** Mobus groups the higher principles (6–12) as characterizing complex adaptive/evolvable systems, with 1–5 holding for any system. The formalization refines this on two counts. *First, the boundary is not at #6.* Blind Evolution (#6) needs only an environmental fitness order and runs on nearly anything — we make a 2-state `Bool` system and the prime-cycle evolvable; Governance (#8) needs a set-point but no model. The genuine complex-adaptive threshold is the **model-bearing line at #9** (a system sophisticated enough to *contain* a model), with #10 (modelling itself) above it. *Second, and more sharply: Understandability (#11) and Improvability (#12) are orthogonal to system complexity.* They track whether an **agent holds a model** and whether the system **admits a compression** — not how complex the system is. The witnesses settle it: `noisyPairUnderstanding` is a *trivial* `Bool × Bool` system that is both understandable and improvable (the agential principles apply to a simple system), while the prime-cycle (`Fin 3`, dynamically richer) is *neither* understandable *nor* improvable, yet still evolvable. So "applies to complex adaptive systems" conflates two independent things — the system being complex, and an agent-with-a-model being present — and the formalization separates them. The demandingness gradient runs: **ontological core (any system) → regulation (#8) → models (#9, #10) → agency (#11, #12)**, with #6 a near-universal *blind* axis off to the side — not a clean cut at 6. *Honest caveat:* this is the *logical/structural* boundary under our minimal cores; Mobus's grouping also makes an empirical point (these principles are *exercised* in real complex adaptive systems) that a formal core does not address — we relocate the logical line, we do not deny the applied one.

    *What this means practically:* A capability check for an agent/tooling layer should not gate the agential operations on "is this system complex?" but on the right preconditions: improvability needs an explicit model + goal; understandability needs a compressing abstraction to exist; evolvability needs only a fitness order. The prime-cycle is the cautionary witness — a system can be intricate and still admit no understanding or directed improvement, while a near-trivial system admits both.

20. **The traditions are generated views of the kernel — K ≅ 2 is now a construction, not just a convergence.** `CommonCore.lean` proved the walking arrow embeds faithfully into all seven shape categories (every tradition *contains* the kernel); `KlirSystem.lean` proved every tradition *projects down* to (T, R). `ViewGeneration.lean` closes the third direction: from the kernel alone, each tradition's presentation is **generated** as a derived view, and the round trips are identities. The kernel is materialized as data (`Kernel`): Klir's (T, R) *plus* the dependency constraint `dep_on` — the walking arrow is exactly the Prop that `KlirSystem` omits (its `relation` field is unconstrained, so a bare KlirSystem is a pair of sets, not yet a system). From a `Kernel`: `toKlir` is the identity-up-to-constraint (`Kernel ↔` dependency-respecting KlirSystem, both round trips `rfl`); `toBunge` generates a *closed* CES triple (E = ∅ — the kernel makes no environmental commitment); `toMobus` generates an 8-tuple with minimal slots (empty external network, no interfaces, `PUnit` parametrics). Every generated view projects back to the kernel unchanged (`toBunge_toKlir`, `toMobus_toKlir`), distinct kernels generate distinct views (all three injectivity lemmas, free from the round trips), and the views cohere: generating Mobus then projecting along the existing bridge equals generating Bunge directly (`toMobus_toBunge`) — the kernel sits at the apex of the commuting triangle. **What generation costs is itself the finding:** the Klir view is free; the Bunge view *forces a bond* (Def 1.1's `bondage_nonempty` reflects back as `Kernel.HasBond` — an empty or bond-free kernel generates no Bunge system: systemhood is not free); the Mobus view *forces irreflexivity* (`no_self_loops`, Mobus's k ≠ o — the engineering view forbids the self-dependency Klir and Bunge tolerate). The "8 slots vs 7" question dissolves: slot count is a property of the view, not the kernel; the invariant is the arrow that all round trips fix.

    *What this means practically:* This is the gating artifact for the BERT data-model refactor ("one kernel model + lens overlays", bert#77): store the kernel, generate the views. A tradition view is a *derived artifact with stated preconditions* — the UI can offer "view as Bunge" only when the model has a bond, "view as Mobus" only when dependencies are irreflexive, and the round-trip lemmas guarantee switching views never mutates the model. The hypotheses are the validation rules, machine-checked.

21. **Convergence entries are claims-at-a-layer — Willems forced the refinement.** Willems' behavioral triple Σ = (T, W, B) entered as a stress test of kernel neutrality: the tradition most hostile to I/O orientation, defining a system as a set of trajectories with no state, no input, no output primitive. The kernel passes — `klirToWillems` (ShapeWillems.lean) embeds the walking arrow because `dep_on` is predication, not causation; the behavioral stance has nothing to refuse. But the same file proves the collapse: as a shape, I_Willems is isomorphic to I_Mesarovic (`willemsToMesarovic`, both directions + object bijection + arrow correspondence), and the iso is forced to send `time ↦ input` and `behavior ↦ globalState` — semantic nonsense, since Willems' entire point is that state is a latent variable of a *representation* (1991 Def VII.1). The absurdity of the forced assignment is the finding: the shape functor measures single-system dependency structure and nothing else, so identity of shape cannot certify identity of commitment. Every convergence entry is therefore a claim *at a layer* — kernel, shape, or composition — and Willems is the first tradition whose independence lives strictly above the shape layer (interconnection as B₁ ∩ B₂, I/O-as-theorem, latent-variable elimination; SSF #16). The headline count stays eight, now with a proof of why counting is layer-relative.

22. **Feedback is a hard boundary of the shape method — Joslyn admits no faithful functor into any category with finite hom-sets.** The acyclic traditions (Klir, Bunge, Mobus, Myers, Wymore, Mesarovic) have DAG-shaped quivers, hence finite hom-sets, and are placed by comparison functors. The two *feedback* traditions are not: Joslyn (feedback through observation, `effector ⇄ controlled`) and Spivak (feedback through value, `parameter ⇄ potential`) both have cyclic quivers with infinite hom-sets. `JoslynIncomparability.lean` proves *why* for Joslyn (the identical pigeonhole covers any cyclic shape, Spivak included): the efferent/afferent feedback cycle makes the free category's hom-set `effector ⟶ effector` **infinite** (the loop `afferent ∘ efferent` and all its powers — `nLoop`, distinguished by `length`), and a faithful functor is injective on hom-sets, so it would inject an infinite set into a finite one — impossible (`joslyn_no_faithful_functor`, pigeonhole via `Finite.of_injective` + `not_finite`). This converts the previously-folklore claim ("Joslyn is categorically incomparable to the DAG-shaped traditions by standard means") into a theorem: the comparison apparatus that places the other seven provably cannot reach Joslyn, and the obstruction is *structural* (cyclicity ⇒ infinite hom-sets), not a gap in effort. Feedback is where the finite-shape method stops. *(Traces/optics were considered as positive tools and rejected — Mathlib has no traced monoidal categories and trace erases the controller/controlled asymmetry that Prop 29 makes load-bearing; the negative theorem is the honest, and higher-value, result.)* Engineering note lives with the proof: **in `Paths V`, drive hom inference through `Quiver.Path`, never through object ascription** (`Paths V := V` lets V's own `Quiver` shadow the path category).

23. **The value maximum differs in kind from the structural ones — value is a non-trivial value *type*, which the kernel cannot supply.** The two-axis ladder (companion 20 + [[convergence-ladder-first-pass]]) has two maxima: Mobus (structural) and Spivak (value). The proposed sharpening was a "Spivak no-section theorem" — but reading `SpivakSystem.lean` refutes it: a section *does* exist (`Kernel.toSpivak` is unconditional, with `toSpivak_toKlir` round-trip and `toSpivak_injective`). The honest distinction is subtler and already half-proven: Bunge's bond and Mobus's irreflexivity are *conditions* on (T, R) — the generate map is **partial** (`HasBond`/`Irreflexive` hypotheses); Spivak's generate is **total but lands in the degenerate stratum** (`toSpivak_potentialFree`, `toSpivak_static` — "all how-it-is, no why-it-moves"). The new general theorem completing it: `SpivakSystem.subsingleton_value_not_driven` — over any *subsingleton* value type (everything the kernel alone carries; `toSpivak` uses `V = PUnit`), no Spivak system is driven; **drivenness requires a non-trivial value type supplied from outside** (`toSpivakWith`). So the value maximum's cost is not a precondition but *data of a new type the kernel does not carry* — the precise, machine-checked form of "conditions vs data" / pointed-vs-unpointed. (`Kernel.toSpivak` is the `V = PUnit` instance of the general theorem.)

    *What this means practically:* When a new tradition is proposed for the convergence table, the first question is "at which layer does its claim live?" — shape-level embedding alone can both under-credit (Willems' real novelty is invisible to shapes) and over-credit (a shape iso to an existing entry means no new single-system commitment). For BERT and the convergence thesis, the layer annotation is load-bearing metadata, not decoration.

24. **Mobus's five life-cycle sections are empty for a formal reason, not a conceptual one — and the fix has to reify well-formedness before it can say anything.** The book-revisions master equation is `S_{t+1} = S_t ∪ ⟨ΔS⟩`, followed by five stage sections with nothing in them. Union is monotone, so `additive_components_monotone` (Lifecycle.lean) gives the exact indictment: under *any* additive regime, no trajectory of any length ever drops a component. Decline and Dissolution are not merely hard to write in that notation — they are unreachable by it. The generous reading is also machine-checked: `growthStep_additive` shows union is not wrong but is `F` restricted to the Growth regime, and Mobus's own `ΔB = ⟨B \ {b_k}, B ∪ {b_new}⟩` already uses set difference, so his notation was ahead of his master equation. The repair is `Step`, an admissible-successor relation, with the closure theorem `wellFormed_of_reaches` over its reflexive-transitive closure. **The methodological finding is separate and sharper: this theorem is vacuous unless well-formedness is stripped out of the type first.** `MobusSystem` carries its five coherence constraints as structure fields, so `F : MobusSystem → Set MobusSystem` makes closure true by construction — the proof is `fun _ h => h`, it compiles, it passes a `#print axioms` audit, and it asserts nothing. Hence `PreTuple` (the tuple as raw data), `WellFormed` as a predicate on it, and adequacy in *both* directions (`wellFormed_toPre` — no stronger than the type; `toMobus`/`toPre_toMobus` — no weaker, the direction that forbids a quietly weakened predicate). Non-vacuity is then witnessed rather than argued: `step_not_additive` exhibits a two-component system with an internal flow 0 → 1 where declining component 0 retracts both the vertex and its edge, so the closure theorem provably ranges over trajectories that lose structure.

    *What this means practically:* A constraint carried as a structure field is unavailable as a *claim* — you cannot prove anything preserves it, because nothing could have violated it. Any SSF result of the form "operation X preserves invariant Y" needs Y reified first, and needs the reification pinned to the type in both directions or the result is about a weaker Y of the prover's choosing. For BERT this is the difference between a validator that rejects malformed models and a theorem that model edits can't malform them.

25. **The tradition-encoding template extends to protocol specifications — Bitcoin enters as the first application probe, and its signature motif is a shape the kernel cannot absorb.** `ShapeBitcoin.lean` encodes Nakamoto (2008) by the same discipline as the eight traditions: positions and arrows drawn from the source's own definitional sentences (§2 "we define an electronic coin as a chain of digital signatures", §3 the timestamp chain), encoding decisions recorded, consensus/proof-of-work explicitly out of scope as a dynamics-layer commitment (the analogue of Willems' composition layer). The kernel embeds (`klirToBitcoin`: things ↦ transaction, relation ↦ coin, along Nakamoto's definitional sentence — object-injective, faithful), and the embedding is strictly one-way (`bitcoin_no_faithful_functor_to_klir`). The obstruction is the finding: hash-chaining is role-level *self*-reference, so `hash_prev : transaction → transaction` and `block_prev : block → block` are generating **endo-arrows** — the first in the landscape. Joslyn's infinite hom-set arises from a composite 2-cycle of two generators; Bitcoin asserts the loop as a single generator, so at the quiver level (where the repaired maximality statement lives) it is a genuinely new shape, not a ride-along — and it fails the Joslyn obstruction (transaction has out-degree 2), consistent with application status rather than kernel candidacy. All five theorems audit to `propext` alone. One-line axiom: *a protocol's ledger is a relation on transactions, and its transactions depend on their own kind — the self-dependency Klir and the traditions never assert.* Probe verdict for the cross-chain program: the encoding does not fight — one import fix, every proof compiled as written; the template's cost structure (source-text discipline, layer annotations) transfers unchanged to protocol texts.

    *What this means practically:* This is the existence proof for "traditions #8 and #9 are protocols" — the first step of the blockchain-isomorphism program. The next probe is a second protocol (an account-model chain) encoded to the same discipline, where the separating question becomes precise: which generating arrows does Ethereum assert that Bitcoin does not, and does the account model's in-place state mutation change the shape or only the dynamics layer?

26. **The UTXO/account-model distinction is a shape-layer theorem — the two protocols put their self-reference on opposite sides of the kernel.** `ShapeEthereum.lean` encodes Wood's Yellow Paper to the same source-text discipline as Bitcoin (§2 "transaction-based state machine", §4.1 the world state as "a mapping between addresses and account states"; gas/EVM/consensus scoped out as dynamics-layer). The kernel embeds (things ↦ account, relation ↦ state, along §4.1's sentence) and refuses to reverse, like Bitcoin — but the endo-generator sits at `state` (σ_{t+1} depends on σ_t), not at `transaction`. The separating instance the protocol program owed (per the standing rule: a new constraint owes a separating instance) is now machine-checked from both sides (`protocol_history_locus_separation`): Bitcoin's things-image has an infinite self-hom while its relation-image (`coin`) is trivial; Ethereum's relation-image has the infinite self-hom while its things-image (`account`) is trivial — and `ethereum_transaction_self` proves Ethereum transactions do not chain, the direct refutation of Bitcoin's `hash_prev` pattern at the same-named role. One-line axiom: *UTXO chains accrete history on the things; the account model accretes it on the relation.* Wood's own design confirms the relocation the shape forces: replay protection lives in the account nonce — in the state — not in transaction back-references. Encoding cost: two compiler iterations, both re-grounds (an instance-synthesis unfold, and the recorded Joslyn engineering note about `Paths V` object ascription firing exactly as documented).

    *What this means practically:* The probe pair is complete — two protocols, one kernel, a provable structural difference stated in the kernel's own vocabulary. This is the theorem-shaped seed for the cross-chain paper and the semantic spine for any TypeDB schema: the schema's UTXO/account split should mirror which role carries the self-referential relation, and a chain's data can now *fail* a shape claim (a UTXO chain exhibiting in-place state mutation, or account transactions that chain).

27. **Solana refines the classification — the history-locus invariant is which ROLE chains, not which kernel side.** `ShapeSolana.lean` encodes Yakovenko's Proof of History to the same discipline (the sequential hash loop with the previous output as next input; event data inserted into the sequence; leaders/verifiers/PoS scoped out as dynamics-layer). The kernel embeds by the constitution reading, exactly Bitcoin's (things ↦ entry, relation ↦ sequence — PoH IS a sequence of computations), and refuses to reverse. The endo-generator sits at the tick (`hash_prev : entry → entry`), which puts Solana in Bitcoin's cell of the 2×2 — things-side history — and the shape still separates them one level finer (`solana_clock_content_separation`): in Bitcoin the chained role and the content role are the SAME position (transactions chain, and transactions are what the ledger is about), while in Solana they are DIFFERENT positions — the chain is a contentless clock, and the content role is provably thin (`solana_event_self`: events do not chain). That is Yakovenko's own pitch (the clock runs before consensus, separated from content) forced out as machine-checked shape. Three protocols now occupy three distinct history loci: value-things (Bitcoin), state-relation (Ethereum), clock-things (Solana) — so the classification invariant that survives three probes is *which role carries the self-reference, and whether that role is the content*. One-line axiom: *Solana chains the clock, not the content — the first protocol whose chained role and content role are distinct positions.* Encoding cost: zero compiler iterations — the file built clean on first attempt; the template has fully amortized. (Quote caveat resolved 2026-08-08: all three protocol files verified verbatim against the published PDFs; two paraphrases tightened, one Yellow Paper section number corrected — see finding 28.)

    *What this means practically:* Three protocols in, the program has a candidate theory, not just pairwise separations: protocol architecture = choice of history locus over the kernel. The falsifiable next probe is a DAG ledger (IOTA/Hedera) or a BFT chain (Tendermint), which should either land in a fourth locus or collapse into an existing one — and either outcome is a result. For the TypeDB layer, the locus is the schema's load-bearing decision: which entity type carries the self-referential relation is now a machine-checked, per-protocol fact that real chain data can violate.

28. **Cosmos answers the classification question by switching families — a ledger is a chain, a network of ledgers is a control system.** `ShapeCosmos.lean` encodes the Cosmos whitepaper (Kwon & Buchman) from source text verified verbatim the same day — this probe was drawn from the actual PDFs/markdown, and the verification pass retro-fitted the first three files (Bitcoin: fully verbatim as written; Ethereum: quotes verbatim but the block section is §4.4 in the current Yellow Paper revision, not §4.3, and "externally" not "external"; Solana: definitional sentence verbatim, two paraphrases tightened). The kernel embeds by the constitution reading ("Cosmos is a network connecting many independent blockchains, called zones" — things ↦ zone, relation ↦ network) and refuses to reverse — but for a NEW reason. Cosmos's quiver has **no endo-generator at all** (`cosmos_no_endo_generator`, axiom-free, against the three chain witnesses in `chain_protocols_have_endo_generators`): no network-layer sentence asserts a role's self-dependency. The infinite hom-set comes instead from the whitepaper's mutual-observation pair, stated verbatim — "the Hub [keeps] up with the state of each zone. Likewise, each zone keeps up with the state of the Hub" — a composite 2-cycle between DISTINCT roles (`syncLoop`, Joslyn's `nLoop` pattern exactly). So the protocol landscape reproduces the tradition landscape's deepest split (finding 22, feedback as the boundary of the shape method): chain protocols sit with the linear traditions (history as an endo-generator, differing in locus — findings 25–27), and the interoperability protocol sits with the control traditions (structure carried by an observer feedback cycle, hub ⇄ zone as efferent/afferent). Encoding decisions recorded: hub-is-a-zone is instance-level while the roles are asymmetric ("zones do not keep up with each other except indirectly through the Hub"); intra-zone Tendermint chaining scoped out as consensus-layer; the instance topology is acyclic while the role dependency is cyclic, and the shape records roles. Content stays thin (`cosmos_packet_self`). One-line axiom: *the interop protocol asserts no self-chaining — its structure is two ledger roles watching each other, which is a control loop, not a chain.*

    *What this means practically:* Four probes have produced a two-family classification with an intra-family invariant: family = chain (endo-generator) vs control (composite cycle), locus = which role carries the loop (value-things / state-relation / clock-things / hub⇄zone). This is now a paper-shaped result — "the shape taxonomy of ledger protocols, machine-checked against their own whitepapers" — and the TypeDB schema design falls out per family: chain protocols get a self-referential relation on one entity type; interop protocols get a symmetric-pair relation between two.

29. **Tendermint proves the family split is a layer split — consensus is a control loop, and what it decides is a chain, in one quiver.** `ShapeTendermint.lean` encodes Buchman–Kwon–Milošević (arXiv:1807.04938) from text verified verbatim before encoding, with a pre-registered prediction logged before reading the paper (control family, ~70%). Outcome: the predicted mechanism is real — PREVOTE is broadcast upon PROPOSAL, and the proposer re-proposes `validValue`, which is set from 2f + 1 PREVOTEs, so proposal ⇄ prevote is a textual composite 2-cycle (`voteLoop`, Joslyn's pattern; the no-faithful-functor pigeonhole runs through it alone) — but the shape is richer than the prediction: §II's blockchain validity clause ("a value is not valid if it does not contain an appropriate hash of the last value (block) added to the blockchain") puts a generating endo-arrow at `value`, and `tendermint_endo_only_at_value` (axiom-free) proves it is the ONLY endo — no message or process role asserts self-dependency. So one quiver carries both taxonomy signatures, split across roles exactly at the decide seam (`tendermint_layer_split`): the control signature on the message roles (how it decides), the chain signature at the decided role (what it decides). The Cosmos finding's two families are the two layers of one stack, and Tendermint is the seam. Encoding decisions recorded: the `chains` endo comes from §II's "in the context of blockchain systems, for example" clause — it is the blockchain instantiation; removing it leaves pure BFT consensus, control-family only. Heights/rounds are message indices, and the 2f + 1 thresholds are conditions-not-shape (finding 23's distinction). One-line axiom: *consensus messages watch each other in a loop; only the decided value chains.* Zero compiler iterations, fifth probe.

    *What this means practically:* The taxonomy now has a compositional reading: a full stack (Cosmos Hub) = control layer (consensus, Tendermint's message cycle) producing a chain layer (the ledger) inside a control layer (hub ⇄ zone interop). Chain and control alternate by level — a Mobus-style hierarchy claim about protocols, machine-checked at each level from each layer's own source text. Pre-registration worked as designed: the prediction was scored against text fetched after it was logged, and the miss (both signatures, not one) is where the finding lives.

30. **The DAG ledger is not a fourth cell — it is Bitcoin's cell with doubled arrows, and branching width becomes the intra-locus invariant.** `ShapeIota.lean` encodes Popov's Tangle (v1.4.3, quotes verified verbatim) to the standing discipline: things ↦ transaction, relation ↦ tangle by the constitution reading ("the transactions issued by nodes constitute the site set of the tangle graph, which is the ledger"), tips and genesis recorded as instance-level features not roles, the MCMC tip-selection scoped out as dynamics. The definitional sentence "when a new transaction arrives, it must approve two previous transactions" yields the landscape's first PARALLEL pair of generating endo-arrows (`approves₁ ≠ approves₂`, axiom-free), at the same locus as Bitcoin: content-things, chained role = content role, relation-image thin (`iota_tangle_self`). The separating theorem (`iota_branching_separation`, fully axiom-free) states both halves: IOTA asserts two distinct parallel endo-generators where Bitcoin's is provably unique (`bitcoin_endo_unique`). So the answer to the DAG probe question is COLLAPSE, informatively: no fourth history locus — the DAG/chain distinction is a branching width within the chain family (Popov's own footnote makes it parametric: "approve k other transactions for a general k ≥ 2"; Bitcoin k = 1, IOTA k = 2). The taxonomy after six probes: two families (chain/control, alternating by layer per finding 29), three loci in the chain family (value-things, state-relation, clock-things), and branching width within each locus. Also recorded: "acyclic" in DAG is instance-level; at the role level the tangle's quiver has self-loops exactly as Bitcoin's does — the same role/instance distinction as Cosmos's acyclic hub topology. One-line axiom: *the tangle is a coin with two previous transactions instead of one — branching width, not history locus, is what a DAG changes.* One compiler iteration (a re-ground: `noConfusion` vs `injection` on indexed families).

    *What this means practically:* The taxonomy is now closed under its first stress test — a candidate fourth family collapsed into a parameter, which is what a good classification does. Paper structure falls out: two families × three loci × branching width, every cell machine-checked from its protocol's own text. The adoption observation gains a structural correlate worth stating carefully in prose (not as a theorem): the DAG ledgers' distinctive commitment is one integer parameter on Bitcoin's shape, which is consistent with — though does not prove — the market treating them as substitutes rather than a new kind.

These findings share a pattern: formalization reveals structure that informal reasoning cannot access. Each finding has both a theoretical and a practical consequence — the theoretical result changes how we understand the principle; the practical consequence informs how BERT, GSR, and Halcyonic modeling workflows should behave. The Lean proofs force the discoveries; the plain-English implications are the deliverable.

---

## Simon's argument, formalized

Simon's "Architecture of Complexity" (1962) is the most cited structural argument in systems science: near-decomposable systems exhibit time-scale separation because strong within-module coupling produces fast internal equilibration, leaving only slow between-module dynamics. Mobus, Bunge, and Klir all reference it. None formalized it.

We built the argument as a chain: **NearDecomposable** (structural partition) → **StrictAnti bridge** (Simon's unstated assumption: stronger interaction → faster dynamics) → **CoupledDynamicSystem** (mutual-influence evolution) → **TimescaleDecomposition** (fast/slow split around equilibrium) → **fast equilibria = product equilibria** (what fast dynamics converges to).

The chain identifies what each principle contributes: Systemness gives composition, Hierarchy gives modules, Dynamics gives the bridge. No single principle produces multi-timescale behavior alone. The formalization also isolates Simon's unnamed assumption, reveals the decomposition is universal (near-decomposability justifies it, doesn't define it), and shows the dynamics hierarchy is fixed-point approximations on product state space.

Full treatment: `docs/reference/simon-argument-formalized.md`

---

## Foundational profile — which results are constructive (open question)

`scripts/axiom-profile.sh` runs `#print axioms` on one showcase theorem per principle — the kernel-computed analogue of a "proof vector" (cf. the Axiom-Based Atlas, arXiv:2504.00063, but *computed* rather than asserted; SSF's 8 systems axioms are structures/defs, not Lean `axiom`s, so this is a foundational-purity signal, not a systems-dependency vector — that lives in `dependency-dag.mmd`). Table in `docs/paper/axiom-table.md`.

Finding: the ontological core is **constructive** (#1, #2, #3, #5, #8, #11 use no axioms; #4, #9, #10, #12 are choice-free — `propext`/`Quot.sound` only; #10's headline moved from the constructive `accurate_invariant` to the stronger, choice-free `FastSelfModel.accurate_forces_periodic` on 2026-07-10). Only **Evolution (#6)** and **Information (#7)** reach `Classical.choice`. For Improvability (#12) the core claim `goal_is_external` is choice-free; only the prime-cycle separation witness `cyclic3_no_directed_improvement` goes classical.

**Open question (not investigated):** is the `Classical.choice` dependence in the #6 and #7 witnesses *essential* to the argument, or *incidental* — inherited from a Mathlib lemma that has a constructive replacement? If incidental, the seam results could be made fully constructive. Flagged, not pursued. *One proven-essential case now exists outside the principles (2026-07-10, `Systems/Joslyn/JoslynSystem.lean`):* Joslyn's Def 21 faithfulness (`Cᶜ = S`) is double-negation elimination — LEM-essential, no constructive proof possible — while the Joslyn→Klir seam theorems in the same file are axiom-free. Essential classicality can sit one definition away from full constructivity. *And one proven-incidental case (same day, `Systems/Joslyn/Control.lean`):* the Control₂ results initially pulled `Classical.choice` purely from Mathlib routing (`Set.singleton_ne_univ`, `Eq.ge` via the Set order instance, `Set.biUnion_singleton`); elementary replacements made all five choice-free. Method: `#print axioms` bisection per lemma, then membership-level rewrites. Both directions of this question are now demonstrated. A `sorryAx`/sorry-reaching check on the build output is the hygiene guard this profile doubles as. *Third case, and a first (2026-07-11, `Systems/Joslyn/BungeMap.lean`):* classicality was **predicted absent during the sketch** — the Joslyn→Bunge map routes through positive membership (support, constructive complement direction), never double complement — and the prediction held: two `Classical.choice` pulls (`Set.inter_compl_self`, `Set.Nonempty.ne_empty`) were Mathlib-routing incidental, eliminated by the same membership-level rewrite method. The essential/incidental question is now *predictable in advance* from the proof route's polarity: negative-to-positive passes (double complement, `Sᶜᶜ = S`) are LEM-essential; positive-membership routes are constructive regardless of how many complements appear in the STATEMENT. *Second confirmation of the polarity heuristic (2026-07-11, `Systems/Joslyn/HCGS.lean`):* the Control₂ ≅ HCGS bridge was called choice-free in the sketch (rfl-tier + singleton membership, no complements) and landed exactly so — three of six declarations fully axiom-free, zero incidental pulls to eliminate this time.

---

## Program status

Nine of twelve principles resolved (~6,910 lines, zero `sorry`):

| # | Principle | Tier | Verdict | Key result |
|---|-----------|------|---------|------------|
| 1 | Systemness | 1 | **Axiom** | Composition closure unconditional at CES and 8-tuple |
| 2 | Hierarchy | 1 | **Axiom** | Simon's implicit assumption named |
| 3 | Networks | 1 | **Axiom** | Complete prior to this program |
| 4 | Dynamics | 2 | **Axiom** (complete, structural) | DynamicSystem, coupled, equilibrium, Flow, timescale decomposition, Simon's bridge |
| 5 | Complexity | 1 | **Theorem** | Derives from #1+#2+#3. First reduction: 12 → ≤11 |
| 6 | Evolution | 3 | **Axiom** (independent), blind | Blind variation+selection over an environmental fitness preorder; `adapts` (fitness non-decreasing across all generations); `Evolvable` (CAES capacity). Needs no model/goal — runs on any system. `evolvable_but_not_improvable`: prime-cycle is evolvable but not improvable (#6 ⇏ #12). Blind counterpart to #12; shared ontogenic skeleton, environmental vs mental selection. |
| 7 | Information | 2 | **Theorem** (layered core) | Genus = difference-that-makes-a-difference (prob-free, knowledge = #9 model); Hartley nonspecificity; Shannon as bounded special case (`entropy ≤ hartley`, equality at uniform). No new axiom. Mutual-info/capacity + semantic layer deferred. |
| 8 | Governance | 2 | **Axiom** + lens bridge | Set point is new structure; Homeostat = lens + setPoint; Conant-Ashby skeleton connects to K ≅ **2** |
| 9 | Internal Models | 2 | **Theorem** | Simulation relation lifts to all horizons (anticipation is structural); model map = Conant-Ashby homomorphism, so #9 supplies #8. Rosen fast model (`AnticipatoryModel`, `lead` steps per tick) races `n·lead` ahead; lockstep = `lead = 1`. No new axiom — second reduction. |
| 10 | Self-Models | 2 | **Theorem** (tractable core) | Diagonal case of #9 (`InternalModel S S`): self-anticipation for free, existence is trivial (identity), accurate self-image is dynamics-invariant. Fast diagonal (`FastSelfModel`): accuracy fights speed — accurate fast self-models force orbit periodicity (`accurate_forces_periodic`); invariance is a lockstep privilege (doubling witness). No new axiom. Faithful-existence (Lawvere/diagonal) deferred. |
| 11 | Understandability | 3 | **Axiom** (independent of #9) | Understanding = strictly-simpler model (onto + lossy + non-degenerate coarse-graining, dynamics commute); predicts all horizons; `card M < card S` & Hartley drop (#7). #9 ⇏ #11 by two witnesses: one-state (nothing to compress) + 3-cycle on `Fin 3` (compression forbidden, 3 prime). `nontrivial` field excludes the degenerate total-collapse. Dual to #9 (encode vs decode). Mobus's "corollary" reversed. |
| 12 | Improvability | 3 | **Axiom** (independent of #6), agential | Improvement = external agent rewrites dynamics so a goal (external set-point) becomes a rest state, stable at all horizons (`persists`). Goal not a function of the system (`goal_is_external`). #12 ⟹ #11 (agent carries an Understanding); #8 governance is the realized engine (`Homeostat.toImprovement`). #12 ⇏ #6: prime-cyclic system has a model but no understanding → no directed agent (`cyclic3_no_directed_improvement`) — evolvable, not engineerable. #11 (GET/observe) + #12 (PUT/intervene) = the agential layer. |

**Status — ALL 12 PRINCIPLES FORMALIZED (6/09).** The program is complete: every Mobus principle has a machine-checked core, zero `sorry`. Verdicts — **Axioms (independent):** #1, #2, #3, #4, #8, #6, #11, #12. **Theorems (derived):** #5 (from #1+#2+#3), #7 (layered, Shannon as special case), #9 (simulation lifts), #10 (diagonal of #9). The headline structural results: the **K ≅ 2 walking arrow** under everything; #5 reduces 12→≤11; the **agential layer** (#11 observe / #12 intervene) splits the 12 into 10 ontological + 2 agential; and the **blind/directed axis** (#6 evolution vs #12 engineering) over structural change. **Next — the deliverable: draw the 12-principle dependency DAG** (axioms vs theorems, with the cross-principle arrows now all justified) — what turns the list into a theory. **Stretches still open** (research-level deepenings, not gaps): (a) Self-Models — a *faithful* self-model via Lawvere's fixed-point theorem (Mathlib has only the powerset diagonal); (b) Information — mutual information, data-processing inequality, channel capacity, the semantic/viability layer; (c) Evolution — explicit populations/replicators, stochastic/expected-fitness selection (Price equation), fitness landscapes.

## Downstream bindings

**2026-07-23 — the typed transition (bert-lenses#112 Half A, step 1).** `Systems/Dynamics/Transition.lean` gives a `Dynamics` descriptor its transition function, TYPED BY THE KIND: `kindCodomain : DynamicsKind → Type → Type` (`deterministic ↝ Id`, `markov ↝ List (X × ℕ)` — the #67 Nat-weighted finite form, no Mathlib distribution type, `nondeterministic ↝ List X`), and `structure Transition (d : Dynamics S)` with `step : d.inputType × S → kindCodomain d.kind (d.outputType × S)` — the coalgebra structure map `S → F(S)` Mealy-wrapped over the descriptor's ports. **The type is the check** — a value of `Transition d` is, by construction, a transition of `d`'s declared kind; no separate gate. **Axiom-free** (`#print axioms` on `kindCodomain`/`deterministicClosed`/`markovClosed`: no dependencies), conservative (adds to `Dynamics`, touches no theorem). Two example constructors — `deterministicClosed` (a closed self-map, the conservation-flow shape) and `markovClosed` (Nat-weighted successors, the #67 absorbing chain) — with `rfl` checks that closed transitions reduce to their underlying data. **Own-the-definition (frontier-council, 2026-07-23):** `kindCodomain` is BERT's own definition; the correspondence to Rutten's coalgebra functors and Spivak's Poly is a comment, never an import, so no moving external theory can invalidate the kernel. Homogeneous/per-kind only; heterogeneous open composition is the research frontier (Half B), deliberately absent. This is the piece that unblocks bert-lenses#100 phase 5 (Klir mask readout = the structure map; Bunge trajectory = its unfolding), #97 (typed dynamics → Bunge's mechanism M), and #67's Markov thread.

**2026-07-23 — MechanismSpec: the honest surrogate for Bunge's M, NOT a CESM retitle (bert-lenses#97 Increment 1).** `Systems/Dynamics/Mechanism.lean`. **What we did NOT do (frontier-council block, triage `2026-07-23/references/type-m-council-triage.md`): we did not retitle the Mobus→Bunge bridge CES → CESM.** Bunge 2004 defines M(σ) *extensionally* — the set of characteristic processes, each a sequence of states, i.e. the behavior set H ⊆ S^ω. The declared dynamics (descriptor + typed transition `T : S → F(S)`) is the *generator* of that set, not the set; calling it M would silently make every "theorem about M" a theorem about the generator (unsound). So Increment 1 ships only the honest, forward-compatible pieces: (a) `structure MechanismSpec (S)` bundling `descriptor : Dynamics S` + `transition : Transition descriptor` as a NAMED AUXILIARY — deliberately not threaded into `MobusSystem.toBunge`, whose proven output stays the CES triple; (b) the Bridge.lean loss-note (τ, item 4) sharpened to say M is *approximated* by MechanismSpec and full M(σ) needs unfolding to H, not yet a Lean type; (c) Bunge's coherence constraint machine-checked — `wellKinded (k : Kingdom) (m : Option (MechanismSpec S)) : Prop := k = conceptual → m.isNone`, i.e. **a conceptual system bears no mechanism (M = ∅, Bunge 2004 :300)**. **Object/model scoping verified (council Q4, the delicate point):** `Kingdom` is asserted on the *modeled object's own nature* (Bunge §1.2: "a theory is a conceptual system, a school a concrete system"), not on what the object processes — SSF has no Lean `Kingdom` type collapsing this; it is a modeler assertion (bert-lenses#71 / `system-type-typologies.md`). So a proof assistant's state machine (concrete artifact representing conceptual objects) has kingdom = `concrete` and the constraint does NOT refuse its mechanism — exercised by the `proofAssistantMechanism` witness; the refusal fires only on a genuinely conceptual object. **Axiom-free** (`#print axioms` on `Kingdom`, `MechanismSpec`, `wellKinded`, `proofAssistantMechanism`: "does not depend on any axioms"), conservative (builds on `Dynamics`/`Transition`, adds no obligation to any existing structure). **Increment 2 (the real #97 close, not done):** type the behavior set H, verify the coiter unfolding from `transition`, discharge the emptiness constraint on H, then and only then retitle CESM.

**2026-07-23 — the declared Dynamics descriptor (bert-lenses#112 / dynamics-position Q5).** `Systems/Dynamics/Record.lean` refines Mobus's opaque `τ` (transform) slot into a typed DESCRIPTOR of a system's dynamics over a carrier: `support · kind · inputType · outputType · invariants`. **Zero new axioms — axiom-free** (`#print axioms` on both example descriptors reports no dependencies at all, not even `propext`/`Quot.sound`; a `structure` plus two enums, proofs by `rfl`/`by decide`). The *declaration ≠ code* principle machine-checked: `kind` names a transition-functor family (`deterministic`/`markov`/`nondeterministic`, grounded in the shipped conservation engine as deterministic + the bert-lenses#67 absorbing-Markov ruling), an engine interprets it — never an embedded per-element script (Mobus's "playful exploration", forfeited because a script is opaque to every validator). **Conservativity by parametricity, witnessed:** `τ` is "carried data with no structural role" (`Tuple.lean:48-49`), so `MobusSystem ... (Dynamics C) ...` typechecks — instantiating `τ := Dynamics C` changes no coherence constraint and breaks no theorem. **Revised after a frontier-council outside pass** (triage: `bert-lenses` session `2026-07-23/references/dynamics-record-council-triage.md`), all findings accepted: (1) `conservation` cut from the kind enum — it is a declared *invariant* (property axis), not a functor family; (2) the opaque per-kind `parameters` slot REMOVED, not left as a landmine — typing it needs the endofunctor, which is #112; (3) `inputType`/`outputType` ports added (default `Unit` = closed) so the record is openness-READY; (4) `support` kept as a tag, time-domain typing deferred. What this is NOT: no endofunctor interpretation, no open-system composition law, no runtime threading — that (with the typed transition/parameters indexed by this descriptor, and openness via Poly / structured cospans / Mealy over the ports) is bert-lenses#112, downstream.

**2026-07-20 — bert-lenses gate binding (Rung 1 of bert-lenses#24).** `Systems/Klir/GatesTruthTable.lean` enumerates every (T,R) kernel over `Fin n` for n ≤ 2 (19 models) and exports the lens-entry verdicts to `bert-lenses/fixtures/gates_truth_table.json`; a Rust CI test asserts bert-core's mode gates agree with the Lean verdicts on every row, both directions. The booleans are tied to the real predicates by `hasBondB_iff` / `irreflexiveB_iff` (ViewGeneration), axioms propext + Quot.sound only — no new axiom, no new principle; this is the kernel's verdicts leaving the repo as machine-checked test vectors rather than prose claims. HasBond is computed under the total-ActsOn instance (where it provably collapses to bert-core's syntactic check); a future non-total ActsOn reopens that gap by design.

**2026-07-20 — bert-lenses#89 decomposition seam (the Lean-first gate).** `Systems/Core/Decomposition.lean` formalizes the three statements gating hierarchical decomposition by reference, over the 8-tuple (`decomposition-foundations.md` §5). **Zero new axioms** — `#print axioms` on `substitution_sound`, `assembleDepth1_wellFormed`, `decompose_one_wellFormed`, `seam_preserves_kind` reports only `propext` + `Quot.sound` (choice-free). What lands:
- **(1) Seam structure — fully.** `structure Decomposition` carries parent tuple, chosen `comp`, child tuple, and the boundary contract as two `Equiv`s (`βsrc : childSources child ≃ inflows parent comp`, `βsnk : childSinks child ≃ outflows parent comp`) so **direction-preservation is by construction**; substance-kind preservation and interface-landing are `Prop` fields; the derived-environment condition `E′ = neighborhood(parent, comp)` is the field `derived_env` — the condition `RecursiveSystem` cannot state and the first-class 8-tuple `E` makes statable.
- **(3) Substitution soundness — fully, structural.** `seam := Equiv.sumCongr βsrc βsnk` is a kind- and direction-preserving bijection between `c`'s parent flows and the child's boundary flows (`seam_bijective`, `seam_preserves_kind`, `seam_inl`/`seam_inr`); each parent flow at `c` continues into exactly one child flow (`inflow_continues`, `outflow_continues`). No `T`/`Δt` claim.
- **(2) Assembly — proven at depth 1.** `assembleDepth1_wellFormed`: a parent + a covering list of well-formed child `RecursiveSystem`s assembles into a `WellFormed` node; `decompose_one_wellFormed` instantiates it with one expanded child among primitive reference-stubs, indifferent to expansion (the "by-reference = in-place" claim, concrete). Arbitrary-depth Mobus→Bunge assembly is a documented conjecture in-file, not formalized.
- **Doc feedback (math followed the Lean).** `FlowEdge` has no substance-kind slot, so property (ii) is only statable by carrying an explicit `kind : FlowEdge α κ → σ` labeling on the `Decomposition` — the kernel step (foundations §7.2) must grow substance kind before the contract check is complete. Also: `inflows`/`outflows` read the seam off `parent.internalNetwork` (`N`), and `childSources`/`childSinks` off `child.externalFlows` (`G′`) by direction — making precise that §3's `flows(c) ⊆ N` are internal-network edges while `Src′/Snk′` are external-flow edges, two different `FlowNetwork`s bridged by `β`.
